# v1.5 Mycelium Sprint — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ship Phase 1 (Hyphal Continuity) of the v1.5-mycelium sprint — extend `src/runtime/workerLifecycle.ts` with observability (stdio→log file), Redis pre-flight ping, explicit `REDIS_URL` env injection, pending-jobs Map for ~100ms bash-crash detection, and a `shutdownInitiated` flag distinguishing clean shutdown from crash. Phases 2–4 (Anamorph Routing, Exoenzyme, Anastomosis) are stubbed at the end of this plan and will receive their own task breakdowns when their respective design amendments land.

**Architecture:** The orchestrator already auto-spawns the BullMQ worker as a managed child via `spawn('pnpm', ['queue:worker'])` and tears it down via SIGTERM→SIGKILL escalation (`src/runtime/workerLifecycle.ts:18`, called at `src/index.ts:191`). v1.5 Phase 1 *extends* this module — does NOT create a new one. Net code delta: ~70-100 LOC added to the existing 56-line file + ~15 LOC at the bash dispatch site + ~10 LOC threading the `WorkerHandle` through `bootTools` + ~8 LOC at the `index.ts` callsite. Tests: one new unit test file at `tests/unit/runtime/workerLifecycle.test.ts`, one new integration test file at `tests/integration/workerLifecycle.test.ts` plus stub workers under `tests/fixtures/workers/`.

**Tech Stack:** Node.js ≥ 20.11, TypeScript strict + `noUncheckedIndexedAccess` + `exactOptionalPropertyTypes`, ESM with NodeNext (`.js` extensions in imports), pnpm, Vitest, Biome. BullMQ over Redis. Existing utilities: `getRedis()` singleton at `src/queue/connection.ts:22`, `Logger` at `src/util/logger.ts`, `node:child_process.spawn`.

**Spec:** `docs/superpowers/specs/v1.5-mycelium-design.md` (HEAD `8118a7d`). All decisions are grounded there; this plan executes the spec.

**Branch:** `feat/phase-1-hyphal-continuity` per `feedback_myceliate_workflow.md`. `--no-ff` summary merge to `main` after review.

**Commit identity:** `~/Myceliate/myceliate-cli` has no git user.name/user.email set. Every `git commit` MUST be prefixed with:

```bash
GIT_AUTHOR_NAME="Patrick" GIT_AUTHOR_EMAIL="amey-jonesp@cardiff.ac.uk" \
GIT_COMMITTER_NAME="Patrick" GIT_COMMITTER_EMAIL="amey-jonesp@cardiff.ac.uk" \
git commit -m "..."
```

---

## Phase 1 review-iteration note (post-merge addendum)

Phase 1 implementation completed at `f1014d9` on `feat/phase-1-hyphal-continuity`. Two-stage parallel review verdict:

- **Spec compliance:** ✅ ACCEPT. Every load-bearing behaviour in spec §2.1–§3.7 verified end-to-end.
- **Code quality:** APPROVE_WITH_FIXES. One critical (WriteStream FD leak) + three important (flag redundancy, unused fixtures, lint noise) applied inline at `f1014d9`.

**Three implementation deviations from this plan, each ACCEPTED in review:**

1. **Tasks T2–T6 collapsed into a single commit** (`26b0052`). All five behaviours present and unit-tested independently; only git-history granularity differs from the per-task pattern. Spec reviewer ACCEPTed; correctness intact.

2. **Task T9 integration tests use real `pnpm queue:worker` + manual SIGKILL** instead of stub-worker fixtures via spawn mock. Root cause: Vitest's `vi.spyOn(cp, 'spawn')` does NOT propagate to named imports (`import { spawn } from 'node:child_process'`) in transitive ESM modules — even with the `vi.importActual` pattern that the plan amendment at `fcc14df` added. The crash-detection assertion (~100ms vs BullMQ's 30s stall) is fully tested; the stub fixtures committed in T9 step 1 were unused and have been deleted in `f1014d9`.

3. **SIGKILL escalation timer (`shutdown()` 2s `setTimeout`) is not exercised end-to-end.** The integration test for it was watered down (it tests a cooperative-SIGTERM worker; real elapsed <3s). The proxy unit test mocks `once('exit')` with `setTimeout(0)` which causes the timer to clear before firing. **Known coverage gap, deferred to v1.5+ phase or v1.6.** The production path (cooperative SIGTERM handled by `worker.ts:27-50`) is covered.

**Source of truth:** the code at HEAD on `feat/phase-1-hyphal-continuity` (then merged to `main`). Tasks T1–T11 below remain accurate as the original execution intent; this note captures the divergences readers should be aware of.

---

## Task 1 — Type contract + async signature refactor (zero behavior change)

**Files:**
- Modify: `src/runtime/workerLifecycle.ts`
- Modify: `src/index.ts:191`
- Create: `tests/unit/runtime/workerLifecycle.test.ts`

This task lays down the new TypeScript surface — error classes, extended `WorkerHandle` type with no-op `trackJob`/`releaseJob` stubs, and an async `startWorker(opts)` signature — without changing runtime behavior. The existing `spawn`/`.resume()`/`shutdown` logic is preserved verbatim. The single callsite at `src/index.ts:191` is updated to `await startWorker({...})`. Build + existing tests must stay green.

- [ ] **Step 1: Create the failing test file**

```ts
// tests/unit/runtime/workerLifecycle.test.ts
import { describe, expect, it } from 'vitest';
import {
  RedisUnavailableError,
  WorkerCrashedError,
} from '../../../src/runtime/workerLifecycle.js';

describe('workerLifecycle — error classes', () => {
  it('RedisUnavailableError carries the canonical diagnostic message', () => {
    const err = new RedisUnavailableError();
    expect(err.name).toBe('RedisUnavailableError');
    expect(err.message).toContain('Redis is required for bash execution');
    expect(err.message).toContain('docker compose up -d redis');
  });

  it('RedisUnavailableError preserves cause', () => {
    const cause = new Error('ECONNREFUSED');
    const err = new RedisUnavailableError(cause);
    expect(err.cause).toBe(cause);
  });

  it('WorkerCrashedError carries exitCode + signal', () => {
    const err = new WorkerCrashedError(1, null);
    expect(err.name).toBe('WorkerCrashedError');
    expect(err.exitCode).toBe(1);
    expect(err.signal).toBeNull();
    expect(err.message).toContain('code=1');
  });
});
```

- [ ] **Step 2: Run test to verify it fails on missing exports**

Run: `pnpm test tests/unit/runtime/workerLifecycle.test.ts`
Expected: FAIL with import error — `RedisUnavailableError` and `WorkerCrashedError` are not exported from `src/runtime/workerLifecycle.ts`.

- [ ] **Step 3: Add error classes + extended WorkerHandle type + async signature to `src/runtime/workerLifecycle.ts`**

Replace the entire current contents of `src/runtime/workerLifecycle.ts` with:

```ts
// src/runtime/workerLifecycle.ts
import { type ChildProcess, spawn } from 'node:child_process';
import type { Logger } from '../util/logger.js';

export type WorkerHandle = {
  child: ChildProcess;
  /** Register an in-flight bash job; lifecycle calls reject() on worker crash. */
  trackJob: (jobId: string, reject: (err: Error) => void) => void;
  /** Bash job completed/failed normally — drop from pending registry. */
  releaseJob: (jobId: string) => void;
  /** SIGTERM → 2s wait → SIGKILL. Idempotent. */
  shutdown: () => Promise<void>;
};

export class RedisUnavailableError extends Error {
  constructor(cause?: Error) {
    super(
      'myceliate: Redis is required for bash execution. ' +
        'Run `docker compose up -d redis` and try again.',
      cause ? { cause } : undefined,
    );
    this.name = 'RedisUnavailableError';
  }
}

export class WorkerCrashedError extends Error {
  constructor(
    public readonly exitCode: number | null,
    public readonly signal: NodeJS.Signals | null,
  ) {
    super(`BullMQ worker crashed (code=${exitCode}, signal=${signal})`);
    this.name = 'WorkerCrashedError';
  }
}

/**
 * Spawn `pnpm queue:worker` as a child subprocess so the bash tool's BullMQ
 * jobs have a consumer. Returns a handle exposing the child, pending-jobs
 * methods, and an idempotent `shutdown` that SIGTERM-then-SIGKILLs.
 *
 * v1.5 Hyphal Continuity additions (vs v1.1 Phase 14):
 * - async signature with redisUrl + logger opts
 * - Redis pre-flight ping (Task 2)
 * - REDIS_URL injected explicitly into child env (Task 3)
 * - stdout/stderr piped to .myceliate/logs/worker.log instead of .resume()-discarded (Task 4)
 * - trackJob/releaseJob pending-jobs Map for ~100ms crash detection (Task 5)
 * - shutdownInitiated flag distinguishing clean shutdown from crash exit (Task 6)
 *
 * Caller is responsible for calling `shutdown()` in the main `try/finally`
 * block alongside `ink.unmount()` and `logger.flush()`.
 */
export async function startWorker(opts: {
  redisUrl: string;
  logger: Logger;
  logsDir: string;
}): Promise<WorkerHandle> {
  // opts referenced for now to satisfy noUnusedParameters; later tasks consume them.
  void opts;

  // Phase 14 review m2 fix: stdout/stderr are 'pipe' (not 'inherit') because
  // the parent has Ink mounted — direct worker writes to the parent's stdout
  // would corrupt the TUI per U4. Pipes are immediately drained via `.resume()`
  // so the ~64KB pipe buffer never fills (which would block the worker on its
  // next write). Task 4 will replace `.resume()` with file-stream piping.
  const child = spawn('pnpm', ['queue:worker'], {
    stdio: ['ignore', 'pipe', 'pipe'],
    detached: false,
  });
  child.stdout?.resume();
  child.stderr?.resume();
  // Phase 14 review m1 fix: spawn 'error' (ENOENT/EACCES) is otherwise an
  // unhandled EventEmitter error that crashes main. Surface it via stderr
  // before Ink mounts.
  child.on('error', (err) => {
    process.stderr.write(`[workerLifecycle] spawn error: ${err.message}\n`);
  });
  let shuttingDown = false;
  const shutdown = async (): Promise<void> => {
    if (shuttingDown) return;
    shuttingDown = true;
    if (child.exitCode !== null) return; // already exited
    child.kill('SIGTERM');
    await new Promise<void>((resolve) => {
      const timer = setTimeout(() => {
        child.kill('SIGKILL');
        resolve();
      }, 2000);
      child.once('exit', () => {
        clearTimeout(timer);
        resolve();
      });
    });
  };
  // No-op stubs — Task 5 implements the real Map-backed pending-jobs registry.
  const trackJob: WorkerHandle['trackJob'] = () => {};
  const releaseJob: WorkerHandle['releaseJob'] = () => {};
  return { child, trackJob, releaseJob, shutdown };
}
```

- [ ] **Step 4: Update the callsite at `src/index.ts:191`**

Find: `const worker = startWorker();`
Replace with:

```ts
const redisUrl = process.env.REDIS_URL ?? 'redis://localhost:6379';   // matches connection.ts:22 default
const worker = await startWorker({
  redisUrl,
  logger,
  logsDir: join(ctx.memoryDir, 'logs'),   // matches createLogger usage at index.ts:63
});
```

(`join` is already imported from `'node:path'` at `src/index.ts:6`.)

- [ ] **Step 5: Run test + typecheck + full test suite to verify the refactor is clean**

Run: `pnpm typecheck && pnpm test --run`
Expected: PASS — new error-class tests green, all 469 pre-existing tests still green, no type errors.

- [ ] **Step 6: Commit**

```bash
GIT_AUTHOR_NAME="Patrick" GIT_AUTHOR_EMAIL="amey-jonesp@cardiff.ac.uk" \
GIT_COMMITTER_NAME="Patrick" GIT_COMMITTER_EMAIL="amey-jonesp@cardiff.ac.uk" \
git add src/runtime/workerLifecycle.ts src/index.ts tests/unit/runtime/workerLifecycle.test.ts && \
git commit -m "feat(workerLifecycle): type contract + async refactor (Task 1, no behavior change)"
```

---

## Task 2 — Redis pre-flight ping

**Files:**
- Modify: `src/runtime/workerLifecycle.ts`
- Modify: `tests/unit/runtime/workerLifecycle.test.ts`

Add `await getRedis(redisUrl).ping()` with a 2s timeout. Failure → throw `RedisUnavailableError(cause)`. The `getRedis()` singleton in `src/queue/connection.ts:22` is reused; no second client.

- [ ] **Step 1: Add failing tests for ping success + failure paths**

Append to `tests/unit/runtime/workerLifecycle.test.ts`:

```ts
import { afterEach, beforeEach, vi } from 'vitest';
import * as connection from '../../../src/queue/connection.js';
import * as childProcess from 'node:child_process';

const mockLogger = {
  info: vi.fn(),
  warn: vi.fn(),
  error: vi.fn(),
  flush: vi.fn(),
} as unknown as import('../../../src/util/logger.js').Logger;

describe('workerLifecycle — Redis pre-flight', () => {
  beforeEach(() => {
    vi.spyOn(childProcess, 'spawn').mockReturnValue({
      stdout: { resume: vi.fn(), pipe: vi.fn() } as never,
      stderr: { resume: vi.fn(), pipe: vi.fn() } as never,
      on: vi.fn(),
      once: vi.fn(),
      kill: vi.fn(),
      exitCode: null,
    } as never);
  });

  afterEach(() => {
    vi.restoreAllMocks();
  });

  it('throws RedisUnavailableError when ping fails', async () => {
    const cause = new Error('ECONNREFUSED');
    vi.spyOn(connection, 'getRedis').mockReturnValue({
      ping: vi.fn().mockRejectedValue(cause),
    } as never);

    const { startWorker } = await import('../../../src/runtime/workerLifecycle.js');
    await expect(
      startWorker({ redisUrl: 'redis://localhost:6379', logger: mockLogger, logsDir: '/tmp/test-logs' }),
    ).rejects.toThrow(/Redis is required for bash execution/);
  });

  it('proceeds to spawn when ping succeeds', async () => {
    vi.spyOn(connection, 'getRedis').mockReturnValue({
      ping: vi.fn().mockResolvedValue('PONG'),
    } as never);

    const { startWorker } = await import('../../../src/runtime/workerLifecycle.js');
    const handle = await startWorker({
      redisUrl: 'redis://localhost:6379',
      logger: mockLogger,
      logsDir: '/tmp/test-logs',
    });
    expect(handle.child).toBeDefined();
    expect(childProcess.spawn).toHaveBeenCalledWith('pnpm', ['queue:worker'], expect.any(Object));
  });
});
```

- [ ] **Step 2: Run test to verify failure case fails for the right reason**

Run: `pnpm test tests/unit/runtime/workerLifecycle.test.ts`
Expected: FAIL — the "throws RedisUnavailableError" test fails because no ping is performed yet (no error is thrown).

- [ ] **Step 3: Implement the ping pre-flight**

In `src/runtime/workerLifecycle.ts`, replace the `void opts;` line at the top of `startWorker` with:

```ts
// Import at top of file (alongside existing imports)
import { getRedis } from '../queue/connection.js';

// Inside startWorker, BEFORE the spawn() call:
try {
  await getRedis(opts.redisUrl).ping();
} catch (err) {
  throw new RedisUnavailableError(err instanceof Error ? err : new Error(String(err)));
}
```

(Remove the `void opts;` line — `opts.redisUrl` is now genuinely consumed.)

- [ ] **Step 4: Run tests to verify both pass**

Run: `pnpm test tests/unit/runtime/workerLifecycle.test.ts`
Expected: PASS — both ping tests + the original error-class tests green.

- [ ] **Step 5: Run full suite to verify no regressions**

Run: `pnpm typecheck && pnpm test --run`
Expected: PASS — all tests green.

- [ ] **Step 6: Commit**

```bash
GIT_AUTHOR_NAME="Patrick" GIT_AUTHOR_EMAIL="amey-jonesp@cardiff.ac.uk" \
GIT_COMMITTER_NAME="Patrick" GIT_COMMITTER_EMAIL="amey-jonesp@cardiff.ac.uk" \
git add src/runtime/workerLifecycle.ts tests/unit/runtime/workerLifecycle.test.ts && \
git commit -m "feat(workerLifecycle): Redis pre-flight ping with RedisUnavailableError (Task 2)"
```

---

## Task 3 — `REDIS_URL` injection into spawn env

**Files:**
- Modify: `src/runtime/workerLifecycle.ts`
- Modify: `tests/unit/runtime/workerLifecycle.test.ts`

Pass `env: { ...process.env, REDIS_URL: opts.redisUrl }` to the `spawn()` call so the worker's `getRedis()` (which reads `process.env.REDIS_URL` per `connection.ts:22`) connects to the *exact* same instance the orchestrator just verified, not whatever happens to be in inherited env.

- [ ] **Step 1: Add failing test for env injection**

Append to `tests/unit/runtime/workerLifecycle.test.ts` inside the existing `describe('workerLifecycle — Redis pre-flight', ...)` block (or a new describe — either works):

```ts
it('injects REDIS_URL into spawned child env', async () => {
  vi.spyOn(connection, 'getRedis').mockReturnValue({
    ping: vi.fn().mockResolvedValue('PONG'),
  } as never);

  const { startWorker } = await import('../../../src/runtime/workerLifecycle.js');
  await startWorker({
    redisUrl: 'redis://my-test-instance:6380',
    logger: mockLogger,
    logsDir: '/tmp/test-logs',
  });

  expect(childProcess.spawn).toHaveBeenCalledWith(
    'pnpm',
    ['queue:worker'],
    expect.objectContaining({
      env: expect.objectContaining({ REDIS_URL: 'redis://my-test-instance:6380' }),
    }),
  );
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `pnpm test tests/unit/runtime/workerLifecycle.test.ts -t "injects REDIS_URL"`
Expected: FAIL — current spawn call has no `env` option, so the assertion on `env.REDIS_URL` fails.

- [ ] **Step 3: Add `env` to the spawn call**

In `src/runtime/workerLifecycle.ts`, change the `spawn()` call from:

```ts
const child = spawn('pnpm', ['queue:worker'], {
  stdio: ['ignore', 'pipe', 'pipe'],
  detached: false,
});
```

to:

```ts
const child = spawn('pnpm', ['queue:worker'], {
  stdio: ['ignore', 'pipe', 'pipe'],
  env: { ...process.env, REDIS_URL: opts.redisUrl },
  detached: false,
});
```

- [ ] **Step 4: Run test to verify it passes**

Run: `pnpm test tests/unit/runtime/workerLifecycle.test.ts -t "injects REDIS_URL"`
Expected: PASS.

- [ ] **Step 5: Full suite check**

Run: `pnpm typecheck && pnpm test --run`
Expected: PASS — all tests green.

- [ ] **Step 6: Commit**

```bash
GIT_AUTHOR_NAME="Patrick" GIT_AUTHOR_EMAIL="amey-jonesp@cardiff.ac.uk" \
GIT_COMMITTER_NAME="Patrick" GIT_COMMITTER_EMAIL="amey-jonesp@cardiff.ac.uk" \
git add src/runtime/workerLifecycle.ts tests/unit/runtime/workerLifecycle.test.ts && \
git commit -m "feat(workerLifecycle): inject REDIS_URL into spawned worker env (Task 3)"
```

---

## Task 4 — Stdio routing to `.myceliate/logs/worker.log`

**Files:**
- Modify: `src/runtime/workerLifecycle.ts`
- Modify: `src/index.ts:191` (pass logsDir into startWorker opts — see below)
- Modify: `tests/unit/runtime/workerLifecycle.test.ts`

Replace the `.resume()` discard pattern with `child.stdout?.pipe(stream); child.stderr?.pipe(stream)` where `stream` is an append-mode write stream to `.myceliate/logs/worker.log`. The `.myceliate/logs/` directory may not exist yet at boot — create it with `mkdirSync(logsDir, { recursive: true })` (idempotent).

The `logsDir` is constructed in `src/index.ts:63` as `join(ctx.memoryDir, 'logs')`. Either: (a) thread `logsDir` into `startWorker` opts, or (b) recompute it inside the lifecycle. Choosing (a) — the logger already owns this path, so the boot sequence threads it cleanly.

- [ ] **Step 1: Add failing test for stdio routing**

Append to `tests/unit/runtime/workerLifecycle.test.ts`:

```ts
import * as fs from 'node:fs';

describe('workerLifecycle — stdio routing', () => {
  beforeEach(() => {
    vi.spyOn(connection, 'getRedis').mockReturnValue({
      ping: vi.fn().mockResolvedValue('PONG'),
    } as never);
  });

  it('pipes child stdout/stderr to worker.log append stream', async () => {
    const stdoutPipe = vi.fn();
    const stderrPipe = vi.fn();
    vi.spyOn(childProcess, 'spawn').mockReturnValue({
      stdout: { pipe: stdoutPipe, resume: vi.fn() } as never,
      stderr: { pipe: stderrPipe, resume: vi.fn() } as never,
      on: vi.fn(),
      once: vi.fn(),
      kill: vi.fn(),
      exitCode: null,
    } as never);

    const mkdirSpy = vi.spyOn(fs, 'mkdirSync').mockImplementation(() => undefined);
    const fakeStream = { write: vi.fn(), end: vi.fn() };
    const createSpy = vi.spyOn(fs, 'createWriteStream').mockReturnValue(fakeStream as never);

    const { startWorker } = await import('../../../src/runtime/workerLifecycle.js');
    await startWorker({
      redisUrl: 'redis://localhost:6379',
      logger: mockLogger,
      logsDir: '/tmp/test-logs',
    });

    expect(mkdirSpy).toHaveBeenCalledWith('/tmp/test-logs', { recursive: true });
    expect(createSpy).toHaveBeenCalledWith('/tmp/test-logs/worker.log', { flags: 'a' });
    expect(stdoutPipe).toHaveBeenCalledWith(fakeStream);
    expect(stderrPipe).toHaveBeenCalledWith(fakeStream);
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `pnpm test tests/unit/runtime/workerLifecycle.test.ts -t "stdio routing"`
Expected: FAIL — `logsDir` opt is not accepted, and `.resume()` is still called instead of `.pipe()`.

- [ ] **Step 3: Add `logsDir` to opts + implement routing**

In `src/runtime/workerLifecycle.ts`:

1. Add to imports:
   ```ts
   import { createWriteStream, mkdirSync } from 'node:fs';
   import { join } from 'node:path';
   ```

2. (`logsDir` is already in the opts type from Task 1 — no signature change needed here.)

3. After the spawn() call but before `child.on('error', ...)`, replace the `.resume()` lines with:
   ```ts
   mkdirSync(opts.logsDir, { recursive: true });
   const workerLog = createWriteStream(join(opts.logsDir, 'worker.log'), { flags: 'a' });
   child.stdout?.pipe(workerLog);
   child.stderr?.pipe(workerLog);
   ```

   Delete the two existing lines:
   ```ts
   child.stdout?.resume();
   child.stderr?.resume();
   ```

- [ ] **Step 4: Update the index.ts:191 callsite to pass logsDir**

In `src/index.ts`, locate the line added in Task 1:

```ts
const worker = await startWorker({ redisUrl, logger });
```

Replace with:

```ts
const worker = await startWorker({
  redisUrl,
  logger,
  logsDir: join(ctx.memoryDir, 'logs'),   // matches createLogger usage at index.ts:63
});
```

- [ ] **Step 5: Run test + full suite**

Run: `pnpm typecheck && pnpm test --run`
Expected: PASS — stdio routing test green, all other tests green.

- [ ] **Step 6: Commit**

```bash
GIT_AUTHOR_NAME="Patrick" GIT_AUTHOR_EMAIL="amey-jonesp@cardiff.ac.uk" \
GIT_COMMITTER_NAME="Patrick" GIT_COMMITTER_EMAIL="amey-jonesp@cardiff.ac.uk" \
git add src/runtime/workerLifecycle.ts src/index.ts tests/unit/runtime/workerLifecycle.test.ts && \
git commit -m "feat(workerLifecycle): route worker stdio to .myceliate/logs/worker.log (Task 4)"
```

---

## Task 5 — Pending-jobs Map (real `trackJob` / `releaseJob`)

**Files:**
- Modify: `src/runtime/workerLifecycle.ts`
- Modify: `tests/unit/runtime/workerLifecycle.test.ts`

Replace the no-op `trackJob`/`releaseJob` stubs from Task 1 with a real `Map<string, (err: Error) => void>` backing. No crash detection wired yet (that's Task 6); this task just sets up the Map state machine.

- [ ] **Step 1: Add failing tests for the Map state transitions**

Append to `tests/unit/runtime/workerLifecycle.test.ts`:

```ts
describe('workerLifecycle — pending jobs Map', () => {
  beforeEach(() => {
    vi.spyOn(connection, 'getRedis').mockReturnValue({
      ping: vi.fn().mockResolvedValue('PONG'),
    } as never);
    vi.spyOn(childProcess, 'spawn').mockReturnValue({
      stdout: { pipe: vi.fn() } as never,
      stderr: { pipe: vi.fn() } as never,
      on: vi.fn(),
      once: vi.fn(),
      kill: vi.fn(),
      exitCode: null,
    } as never);
    vi.spyOn(fs, 'mkdirSync').mockImplementation(() => undefined);
    vi.spyOn(fs, 'createWriteStream').mockReturnValue({} as never);
  });

  it('trackJob + releaseJob cycle does not throw', async () => {
    const { startWorker } = await import('../../../src/runtime/workerLifecycle.js');
    const handle = await startWorker({
      redisUrl: 'redis://localhost:6379',
      logger: mockLogger,
      logsDir: '/tmp/test-logs',
    });
    const reject = vi.fn();
    expect(() => handle.trackJob('job-1', reject)).not.toThrow();
    expect(() => handle.releaseJob('job-1')).not.toThrow();
  });

  it('releaseJob on unknown jobId is a no-op (does not throw)', async () => {
    const { startWorker } = await import('../../../src/runtime/workerLifecycle.js');
    const handle = await startWorker({
      redisUrl: 'redis://localhost:6379',
      logger: mockLogger,
      logsDir: '/tmp/test-logs',
    });
    expect(() => handle.releaseJob('never-tracked')).not.toThrow();
  });
});
```

- [ ] **Step 2: Run test to verify the Map presence is irrelevant (stubs pass these trivially)**

Run: `pnpm test tests/unit/runtime/workerLifecycle.test.ts -t "pending jobs Map"`
Expected: PASS even with no-op stubs (trackJob/releaseJob currently do nothing). These tests document the contract; Task 6 will add behavior tests that distinguish stub from impl.

- [ ] **Step 3: Implement the Map**

In `src/runtime/workerLifecycle.ts`, inside `startWorker` BEFORE the `return` statement, replace the no-op stubs:

```ts
// No-op stubs — Task 5 implements the real Map-backed pending-jobs registry.
const trackJob: WorkerHandle['trackJob'] = () => {};
const releaseJob: WorkerHandle['releaseJob'] = () => {};
```

with:

```ts
const pendingJobs = new Map<string, (err: Error) => void>();
const trackJob: WorkerHandle['trackJob'] = (jobId, reject) => {
  pendingJobs.set(jobId, reject);
};
const releaseJob: WorkerHandle['releaseJob'] = (jobId) => {
  pendingJobs.delete(jobId);
};
```

(The Map will be consumed by the crash handler in Task 6.)

- [ ] **Step 4: Run tests + typecheck**

Run: `pnpm typecheck && pnpm test --run`
Expected: PASS — all tests green.

- [ ] **Step 5: Commit**

```bash
GIT_AUTHOR_NAME="Patrick" GIT_AUTHOR_EMAIL="amey-jonesp@cardiff.ac.uk" \
GIT_COMMITTER_NAME="Patrick" GIT_COMMITTER_EMAIL="amey-jonesp@cardiff.ac.uk" \
git add src/runtime/workerLifecycle.ts tests/unit/runtime/workerLifecycle.test.ts && \
git commit -m "feat(workerLifecycle): pending-jobs Map for trackJob/releaseJob (Task 5)"
```

---

## Task 6 — `shutdownInitiated` flag + crash handler

**Files:**
- Modify: `src/runtime/workerLifecycle.ts`
- Modify: `tests/unit/runtime/workerLifecycle.test.ts`

Add a `shutdownInitiated: boolean` flag set to `true` as the first line of `shutdown()`. Add a `child.on('exit', ...)` handler that distinguishes:
- `shutdownInitiated === true` → clean shutdown path (log `worker_shutdown_complete`, no rejections)
- `shutdownInitiated === false` AND (`code !== 0` OR pending jobs exist) → crash path (iterate pending Map, call each `reject(new WorkerCrashedError(code, signal))`, log `worker_crashed`)

- [ ] **Step 1: Add failing tests for clean vs crash exit paths**

Append to `tests/unit/runtime/workerLifecycle.test.ts`:

```ts
describe('workerLifecycle — crash detection', () => {
  beforeEach(() => {
    vi.spyOn(connection, 'getRedis').mockReturnValue({
      ping: vi.fn().mockResolvedValue('PONG'),
    } as never);
    vi.spyOn(fs, 'mkdirSync').mockImplementation(() => undefined);
    vi.spyOn(fs, 'createWriteStream').mockReturnValue({} as never);
  });

  it('rejects all tracked jobs on non-clean exit', async () => {
    let exitHandler!: (code: number | null, signal: NodeJS.Signals | null) => void;
    vi.spyOn(childProcess, 'spawn').mockReturnValue({
      stdout: { pipe: vi.fn() } as never,
      stderr: { pipe: vi.fn() } as never,
      on: vi.fn((event, handler) => {
        if (event === 'exit') exitHandler = handler;
      }),
      once: vi.fn(),
      kill: vi.fn(),
      exitCode: null,
    } as never);

    const { startWorker, WorkerCrashedError } = await import('../../../src/runtime/workerLifecycle.js');
    const handle = await startWorker({
      redisUrl: 'redis://localhost:6379',
      logger: mockLogger,
      logsDir: '/tmp/test-logs',
    });

    const reject1 = vi.fn();
    const reject2 = vi.fn();
    handle.trackJob('job-1', reject1);
    handle.trackJob('job-2', reject2);

    // Simulate worker crash
    exitHandler(1, null);

    expect(reject1).toHaveBeenCalledWith(expect.any(WorkerCrashedError));
    expect(reject2).toHaveBeenCalledWith(expect.any(WorkerCrashedError));
    expect(reject1.mock.calls[0]![0].exitCode).toBe(1);
  });

  it('does NOT reject tracked jobs on clean shutdown exit', async () => {
    let exitHandler!: (code: number | null, signal: NodeJS.Signals | null) => void;
    vi.spyOn(childProcess, 'spawn').mockReturnValue({
      stdout: { pipe: vi.fn() } as never,
      stderr: { pipe: vi.fn() } as never,
      on: vi.fn((event, handler) => {
        if (event === 'exit') exitHandler = handler;
      }),
      once: vi.fn((event, handler) => {
        if (event === 'exit') {
          // simulate immediate exit on shutdown
          setTimeout(() => handler(0, 'SIGTERM'), 0);
        }
      }),
      kill: vi.fn(),
      exitCode: null,
    } as never);

    const { startWorker } = await import('../../../src/runtime/workerLifecycle.js');
    const handle = await startWorker({
      redisUrl: 'redis://localhost:6379',
      logger: mockLogger,
      logsDir: '/tmp/test-logs',
    });

    const reject = vi.fn();
    handle.trackJob('job-1', reject);

    await handle.shutdown();
    // Now simulate the actual exit event the kernel would deliver
    exitHandler(0, 'SIGTERM');

    expect(reject).not.toHaveBeenCalled();
  });
});
```

- [ ] **Step 2: Run tests to verify failures**

Run: `pnpm test tests/unit/runtime/workerLifecycle.test.ts -t "crash detection"`
Expected: FAIL — no `child.on('exit')` handler is registered, so `exitHandler` is undefined and the first test throws.

- [ ] **Step 3: Add the flag + exit handler**

In `src/runtime/workerLifecycle.ts`:

1. Inside `startWorker`, before the `let shuttingDown = false;` line, add:

   ```ts
   let shutdownInitiated = false;
   ```

2. Inside the `shutdown` async function, change the first line from:

   ```ts
   if (shuttingDown) return;
   shuttingDown = true;
   ```

   to:

   ```ts
   shutdownInitiated = true;
   if (shuttingDown) return;
   shuttingDown = true;
   ```

3. After the existing `child.on('error', ...)` handler, add:

   ```ts
   child.on('exit', (code, signal) => {
     if (shutdownInitiated) {
       opts.logger.info({ event: 'worker_shutdown_complete', code, signal });
       return;
     }
     if (code !== 0 || pendingJobs.size > 0) {
       const pendingIds = [...pendingJobs.keys()];
       opts.logger.error({ event: 'worker_crashed', exitCode: code, signal, pendingJobs: pendingIds });
       const err = new WorkerCrashedError(code, signal);
       for (const reject of pendingJobs.values()) {
         reject(err);
       }
       pendingJobs.clear();
     }
   });
   ```

- [ ] **Step 4: Run tests to verify both pass**

Run: `pnpm test tests/unit/runtime/workerLifecycle.test.ts -t "crash detection"`
Expected: PASS.

- [ ] **Step 5: Full suite + typecheck**

Run: `pnpm typecheck && pnpm test --run`
Expected: PASS — all tests green.

- [ ] **Step 6: Commit**

```bash
GIT_AUTHOR_NAME="Patrick" GIT_AUTHOR_EMAIL="amey-jonesp@cardiff.ac.uk" \
GIT_COMMITTER_NAME="Patrick" GIT_COMMITTER_EMAIL="amey-jonesp@cardiff.ac.uk" \
git add src/runtime/workerLifecycle.ts tests/unit/runtime/workerLifecycle.test.ts && \
git commit -m "feat(workerLifecycle): shutdownInitiated flag + crash handler with WorkerCrashedError fan-out (Task 6)"
```

---

## Task 7 — Thread `worker: WorkerHandle` through `BashToolDeps` + `bootTools` (no dispatch change yet)

**Files:**
- Modify: `src/tools/bash.ts` (add `worker` to `BashToolDeps`, no dispatch change)
- Modify: `src/runtime/bootTools.ts` (add `worker` to `BootToolsOpts`, thread to bash registration)
- Modify: `src/index.ts` (pass `worker` to `bootTools`)
- Modify: `tests/unit/runtime/bootTools.test.ts` if it constructs `BootToolsOpts` (add `worker` stub)

This task changes types only — wires the `worker` handle through the dependency chain without modifying the bash dispatch logic. Task 8 will then use the now-available `worker` handle in the dispatch.

- [ ] **Step 1: Add `worker` to `BashToolDeps`**

In `src/tools/bash.ts`, change:

```ts
export type BashToolDeps = {
  hitl: HitlGate;
  queue: Queue<BashJobData, BashJobReturn>;
  queueEvents: QueueEvents;
  defaultTimeoutMs?: number;
};
```

to:

```ts
import type { WorkerHandle } from '../runtime/workerLifecycle.js';

export type BashToolDeps = {
  hitl: HitlGate;
  queue: Queue<BashJobData, BashJobReturn>;
  queueEvents: QueueEvents;
  worker: WorkerHandle;
  defaultTimeoutMs?: number;
};
```

- [ ] **Step 2: Add `worker` to `BootToolsOpts` and thread to bash registration**

In `src/runtime/bootTools.ts`, add to `BootToolsOpts`:

```ts
import type { WorkerHandle } from './workerLifecycle.js';

export interface BootToolsOpts {
  hitl: HitlGate;
  queue?: Queue<BashJobData, BashJobReturn>;
  queueEvents?: QueueEvents;
  worker?: WorkerHandle;   // NEW — required for real bash registration
  registry: SporeRegistry;
  // ... rest unchanged
}
```

Update the bash registration condition (currently `if (opts.queue && opts.queueEvents)`) to:

```ts
if (opts.queue && opts.queueEvents && opts.worker) {
  tools.register(
    createBashTool({
      hitl: opts.hitl,
      queue: opts.queue,
      queueEvents: opts.queueEvents,
      worker: opts.worker,
      defaultTimeoutMs: 30_000,
    }),
  );
} else {
  // existing stub registration unchanged
}
```

- [ ] **Step 3: Pass `worker` from `index.ts` into `bootTools`**

In `src/index.ts`, find the `bootTools({ ... })` call (around line 196) and add `worker` to the opts:

```ts
const { tools, setActiveSpore } = bootTools({
  hitl,
  queue,
  queueEvents,
  worker,                         // NEW
  registry: spores.registry,
  cwd,
  logger,
  // ... rest unchanged
});
```

- [ ] **Step 4: Run typecheck — fixes any test files that construct `BashToolDeps` directly**

Run: `pnpm typecheck`
Expected: type errors in any test file that constructs `BashToolDeps` or `BootToolsOpts` without `worker`. Likely candidates: `tests/unit/runtime/bootTools.test.ts`, `tests/integration/bashToolE2E.test.ts`, `tests/integration/queueRoundTrip.test.ts`.

For each failing test, add a stub `worker`:

```ts
const stubWorker = {
  child: {} as never,
  trackJob: () => {},
  releaseJob: () => {},
  shutdown: async () => {},
};
```

and pass it where needed.

- [ ] **Step 5: Run full suite to verify no regressions**

Run: `pnpm typecheck && pnpm test --run`
Expected: PASS — no type errors, no test regressions.

- [ ] **Step 6: Commit**

```bash
GIT_AUTHOR_NAME="Patrick" GIT_AUTHOR_EMAIL="amey-jonesp@cardiff.ac.uk" \
GIT_COMMITTER_NAME="Patrick" GIT_COMMITTER_EMAIL="amey-jonesp@cardiff.ac.uk" \
git add src/tools/bash.ts src/runtime/bootTools.ts src/index.ts tests/ && \
git commit -m "refactor: thread WorkerHandle through BashToolDeps + bootTools (Task 7, no dispatch change)"
```

---

## Task 8 — Bash dispatch: `Promise.race` for ~100ms crash detection

**Files:**
- Modify: `src/tools/bash.ts:78-89` (the dispatch block)
- Add tests verifying the race semantics

Replace the current `await job.waitUntilFinished(deps.queueEvents)` call with a `Promise.race` against a `crashPromise` registered via `deps.worker.trackJob`. Wrap in `try/finally` so `releaseJob` always fires.

- [ ] **Step 1: Add a unit test for the race semantics**

Create or extend `tests/unit/tools/bash.test.ts` (mirror the file's path under `src/tools/`). If the file does not exist, create it:

```ts
// tests/unit/tools/bash.test.ts
import { describe, expect, it, vi } from 'vitest';
import { createBashTool, type BashToolDeps } from '../../../src/tools/bash.js';
import { WorkerCrashedError } from '../../../src/runtime/workerLifecycle.js';

describe('bash tool — worker crash detection', () => {
  it('rejects with WorkerCrashedError when the worker crashes mid-dispatch', async () => {
    let registeredReject!: (err: Error) => void;
    const trackJob = vi.fn((_: string, reject: (err: Error) => void) => {
      registeredReject = reject;
    });
    const releaseJob = vi.fn();

    const fakeJob = {
      waitUntilFinished: vi.fn(() => new Promise(() => {/* never resolves */})),
    };

    const deps: BashToolDeps = {
      hitl: { checkBash: async () => ({ allowed: true }) } as never,
      queue: { add: vi.fn().mockResolvedValue(fakeJob) } as never,
      queueEvents: {} as never,
      worker: {
        child: {} as never,
        trackJob,
        releaseJob,
        shutdown: async () => {},
      },
      defaultTimeoutMs: 30_000,
    };

    const tool = createBashTool(deps);
    const dispatchPromise = tool.run(
      { command: 'echo hi', cwd: '', timeoutMs: 0 },
      { cwd: '/tmp', toolUseId: 'test-id' } as never,
    );

    // Simulate worker crash by invoking the registered rejecter
    setTimeout(() => registeredReject(new WorkerCrashedError(1, null)), 5);

    await expect(dispatchPromise).rejects.toThrow(WorkerCrashedError);
    expect(releaseJob).toHaveBeenCalledWith(expect.any(String));
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `pnpm test tests/unit/tools/bash.test.ts`
Expected: FAIL — current dispatch awaits `waitUntilFinished` which never resolves; the test times out (or hangs) instead of rejecting.

- [ ] **Step 3: Modify the bash dispatch block**

In `src/tools/bash.ts`, replace lines 77-89 (the current dispatch — `randomUUID`, `queue.add`, `waitUntilFinished`, `formatResult`):

```ts
const toolUseId = randomUUID();
const job = await deps.queue.add(
  'bash',
  {
    command: input.command,
    cwd,
    timeoutMs,
    toolUseId,
  },
  { jobId: toolUseId },
);
const result = await job.waitUntilFinished(deps.queueEvents);
return formatResult(result);
```

with:

```ts
const toolUseId = randomUUID();
const job = await deps.queue.add(
  'bash',
  {
    command: input.command,
    cwd,
    timeoutMs,
    toolUseId,
  },
  { jobId: toolUseId },
);
let crashReject!: (err: Error) => void;
const crashPromise = new Promise<never>((_, reject) => {
  crashReject = reject;
});
deps.worker.trackJob(toolUseId, crashReject);
try {
  const result = await Promise.race([
    job.waitUntilFinished(deps.queueEvents),
    crashPromise,
  ]);
  return formatResult(result);
} finally {
  deps.worker.releaseJob(toolUseId);
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `pnpm test tests/unit/tools/bash.test.ts`
Expected: PASS — bash dispatch now rejects with `WorkerCrashedError` within milliseconds of the simulated crash.

- [ ] **Step 5: Full suite + typecheck**

Run: `pnpm typecheck && pnpm test --run`
Expected: PASS — all tests green.

- [ ] **Step 6: Commit**

```bash
GIT_AUTHOR_NAME="Patrick" GIT_AUTHOR_EMAIL="amey-jonesp@cardiff.ac.uk" \
GIT_COMMITTER_NAME="Patrick" GIT_COMMITTER_EMAIL="amey-jonesp@cardiff.ac.uk" \
git add src/tools/bash.ts tests/unit/tools/bash.test.ts && \
git commit -m "feat(bash): Promise.race against worker crash for ~100ms detection (Task 8)"
```

---

## Task 9 — Integration test with real Redis + stub workers

**Files:**
- Create: `tests/fixtures/workers/exitNonZero.ts`
- Create: `tests/fixtures/workers/hangForever.ts`
- Create: `tests/integration/workerLifecycle.test.ts`

End-to-end verification with real Redis. Stub workers replace the production worker in each test scenario. Skipped when `REDIS_URL` is unset.

- [ ] **Step 1: Create the stub workers**

Create `tests/fixtures/workers/exitNonZero.ts`:

```ts
// tests/fixtures/workers/exitNonZero.ts
// Stub worker that exits 1 immediately. Used in workerLifecycle integration tests
// to simulate a crashed worker and verify the orchestrator's pending-jobs Map
// rejects in-flight bash promises within ~100ms instead of waiting BullMQ's stall.
console.log('[stub-worker] exiting 1');
process.exit(1);
```

Create `tests/fixtures/workers/hangForever.ts`:

```ts
// tests/fixtures/workers/hangForever.ts
// Stub worker that ignores SIGTERM and hangs. Used to verify the SIGKILL
// escalation path in shutdown().
console.log('[stub-worker] hanging; ignoring SIGTERM');
process.on('SIGTERM', () => console.log('[stub-worker] received SIGTERM, ignoring'));
setInterval(() => {}, 1_000_000);
```

- [ ] **Step 2: Create the integration test file**

Create `tests/integration/workerLifecycle.test.ts`:

```ts
// tests/integration/workerLifecycle.test.ts
import { fileURLToPath } from 'node:url';
import { afterEach, describe, expect, it, vi } from 'vitest';
import { createLogger } from '../../src/util/logger.js';

const REDIS_URL = process.env.REDIS_URL;

// ESM-safe sibling-path resolution (no __dirname in ESM).
const exitNonZeroPath = fileURLToPath(new URL('../fixtures/workers/exitNonZero.ts', import.meta.url));
const hangForeverPath = fileURLToPath(new URL('../fixtures/workers/hangForever.ts', import.meta.url));

describe.skipIf(!REDIS_URL)('workerLifecycle integration', () => {
  const logger = createLogger({ logsDir: '/tmp/myceliate-test-logs' });

  afterEach(() => {
    vi.restoreAllMocks();
  });

  it('rejects pending bash promises within 200ms of stub worker crash', async () => {
    // Redirect the spawn target to the stub worker fixture instead of pnpm queue:worker.
    // Dynamic-import the child_process module so the spy applies to the same module
    // object the lifecycle's `import { spawn } from 'node:child_process'` resolves to.
    // CRITICAL: use vi.importActual to grab the un-spied original implementation —
    // calling cp.spawn from inside the mockImplementation would recurse into the
    // spy itself and hit "Maximum call stack size exceeded".
    const cp = await import('node:child_process');
    const actualCp = await vi.importActual<typeof import('node:child_process')>('node:child_process');
    vi.spyOn(cp, 'spawn').mockImplementation(
      ((_cmd: string, _args: readonly string[], opts: import('node:child_process').SpawnOptions) =>
        actualCp.spawn('tsx', [exitNonZeroPath], opts)) as never,
    );

    const { startWorker, WorkerCrashedError } = await import('../../src/runtime/workerLifecycle.js');
    const handle = await startWorker({
      redisUrl: REDIS_URL!,
      logger,
      logsDir: '/tmp/myceliate-test-logs',
    });

    const reject = vi.fn();
    handle.trackJob('test-job', reject);

    // Wait for stub to crash + exit handler to fire
    await new Promise((r) => setTimeout(r, 200));

    expect(reject).toHaveBeenCalledWith(expect.any(WorkerCrashedError));
    await handle.shutdown();
  });

  it('SIGKILL escalation kills a stub that ignores SIGTERM', async () => {
    // Same vi.importActual pattern as above to avoid recursive spawn into the spy.
    const cp = await import('node:child_process');
    const actualCp = await vi.importActual<typeof import('node:child_process')>('node:child_process');
    vi.spyOn(cp, 'spawn').mockImplementation(
      ((_cmd: string, _args: readonly string[], opts: import('node:child_process').SpawnOptions) =>
        actualCp.spawn('tsx', [hangForeverPath], opts)) as never,
    );

    const { startWorker } = await import('../../src/runtime/workerLifecycle.js');
    const handle = await startWorker({
      redisUrl: REDIS_URL!,
      logger,
      logsDir: '/tmp/myceliate-test-logs',
    });

    const start = Date.now();
    await handle.shutdown();
    const elapsed = Date.now() - start;

    expect(elapsed).toBeGreaterThanOrEqual(2000); // SIGTERM wait
    expect(elapsed).toBeLessThan(3000); // SIGKILL kicks in at 2s
    expect(handle.child.killed).toBe(true);
  });
});
```

- [ ] **Step 3: Run the integration tests with Redis available**

```bash
docker compose up -d redis
REDIS_URL=redis://localhost:6379 pnpm test tests/integration/workerLifecycle.test.ts
```

Expected: PASS — both tests green within ~5 seconds total.

- [ ] **Step 4: Run the full suite to confirm no regressions**

Run: `pnpm typecheck && pnpm test --run` (without REDIS_URL — integration tests skip)
Then: `REDIS_URL=redis://localhost:6379 pnpm test --run` (with REDIS_URL — integration tests run)
Expected: PASS in both modes.

- [ ] **Step 5: Commit**

```bash
GIT_AUTHOR_NAME="Patrick" GIT_AUTHOR_EMAIL="amey-jonesp@cardiff.ac.uk" \
GIT_COMMITTER_NAME="Patrick" GIT_COMMITTER_EMAIL="amey-jonesp@cardiff.ac.uk" \
git add tests/fixtures/workers/ tests/integration/workerLifecycle.test.ts && \
git commit -m "test(integration): real-Redis worker crash + SIGKILL escalation (Task 9)"
```

---

## Task 10 — Doc updates

**Files:**
- Modify: `docs/MANUAL_SMOKE.md`
- Modify: `README.md`
- Modify: `src/queue/worker.ts` (top-of-file comment)

- [ ] **Step 1: Update `docs/MANUAL_SMOKE.md`**

Add a new walk-point 0 at the top of the walk-points section:

```markdown
### Walk-point 0 — Worker auto-spawn observability (NEW v1.5)

1. Boot myceliate fresh: `myceliate` (no flags).
2. From another terminal, run: `cat .myceliate/logs/worker.log`
3. **Expect:** the log file exists and contains the line `[worker] consuming queue: bash (concurrency=4)`.
4. If the file does not exist or is empty: the worker did not spawn correctly. Check Redis is up (`docker compose ps redis`), then check the orchestrator's stderr for any `[workerLifecycle] spawn error:` line.
```

If MANUAL_SMOKE.md still references "Terminal A: pnpm queue:worker" anywhere in walk-points 1-8, remove those instructions — the worker is auto-spawned. Replace with: "Worker is auto-spawned by the orchestrator at boot; check `.myceliate/logs/worker.log` if bash hangs, fails to dispatch, or produces unexpected output."

- [ ] **Step 2: Update `README.md`**

In the architecture section (or quickstart, depending on existing structure), add a paragraph:

```markdown
### Worker process lifecycle

The orchestrator owns the BullMQ worker process lifecycle. At boot, `myceliate`
runs a Redis pre-flight ping (fail-fast with a clean diagnostic if Redis is
down), spawns `pnpm queue:worker` as a managed child process, routes its
stdout/stderr to `.myceliate/logs/worker.log`, and tears it down on orchestrator
exit via SIGTERM → 2s wait → SIGKILL escalation. If a bash command hangs or
fails unexpectedly, tail the worker log: `tail -f .myceliate/logs/worker.log`.
```

If the quickstart references "open a second terminal and run `pnpm queue:worker`", remove that instruction.

- [ ] **Step 3: Add the worker.ts top comment**

In `src/queue/worker.ts`, change the existing first line (`// src/queue/worker.ts`) to:

```ts
// src/queue/worker.ts
//
// BullMQ worker entry point. Two ways this gets invoked:
//   (1) Indirectly by orchestrator boot via startWorker() in src/runtime/workerLifecycle.ts.
//       This is the production path; stdio is routed to .myceliate/logs/worker.log.
//   (2) Manually as `pnpm queue:worker` in a developer terminal when an implementer
//       wants to see live worker output without the log-file routing layer
//       (e.g. attaching a debugger, watching stderr in real time).
```

- [ ] **Step 4: Verify docs render cleanly**

Run: `cat docs/MANUAL_SMOKE.md README.md src/queue/worker.ts | head -200`
Expected: no markdown syntax errors; comment block in worker.ts is on lines 1-9.

- [ ] **Step 5: Commit**

```bash
GIT_AUTHOR_NAME="Patrick" GIT_AUTHOR_EMAIL="amey-jonesp@cardiff.ac.uk" \
GIT_COMMITTER_NAME="Patrick" GIT_COMMITTER_EMAIL="amey-jonesp@cardiff.ac.uk" \
git add docs/MANUAL_SMOKE.md README.md src/queue/worker.ts && \
git commit -m "docs: walk-point 0 (worker.log smoke), README architecture note, worker.ts dual-purpose comment (Task 10)"
```

---

## Task 11 — Manual smoke regression (verification only, no commit)

**Files:** none touched.

- [ ] **Step 1: Build + global link refresh**

```bash
pnpm build && pnpm link --global
```

Expected: clean build, global `myceliate` symlink updated.

- [ ] **Step 2: Boot and verify the log file appears**

```bash
cd /tmp && rm -rf .myceliate
myceliate
```

In a second terminal:

```bash
ls -la /tmp/.myceliate/logs/
cat /tmp/.myceliate/logs/worker.log
```

Expected: `worker.log` exists. Contains `[worker] consuming queue: bash (concurrency=4)`.

- [ ] **Step 3: Run a bash command end-to-end**

Inside the running myceliate session, prompt:

> Run `echo hello world` via the bash tool.

Expected: tool fires, returns `exitCode: 0` and `stdout: hello world`.

- [ ] **Step 4: Simulate worker crash**

In a third terminal: `pkill -f 'queue:worker'`

Then back in myceliate, prompt another bash command. Expected: bash fails within ~200ms with a `WorkerCrashedError` message surfaced to the orchestrator. The orchestrator does NOT hang for 30 seconds.

- [ ] **Step 5: Check the log captured the crash**

```bash
tail /tmp/.myceliate/logs/worker.log
```

Expected: worker startup line, then the `pkill` should have caused a non-zero exit. The orchestrator's logger should have emitted a `worker_crashed` event.

- [ ] **Step 6: Clean exit**

Ctrl+C myceliate. Expected: clean shutdown within 2s (SIGTERM honoured by the auto-spawned worker via its existing handler at `src/queue/worker.ts:27-50`).

---

## Phase 2 — Anamorph Routing

**Goal:** Add a Spore-role-to-model dispatcher. Per-call dispatch via `roleToModel(role: SporeRole)` routes Anamorph roles (mechanical execution) to V4-Flash and Teleomorph roles (strategic / reasoning-active turns) to V4-Pro. R2 reasoning ratchet via `QueryEngine.hasRetainedReasoning()`. Iteration 0 of every orchestrator REPL turn unconditionally lands on Pro (planning bias per Gemini's round-2 review). `DEEPSEEK_MODEL` env override preserved with an unmissable boot-time warn line.

**Architecture:** Two wire-touch sites confirmed by `grep -rn "client\.stream(" src` — `src/orchestrator/reactLoop.ts:48` and `src/spores/subagentLoop.ts:64`. Phase 2 adds one new module (`src/runtime/roleToModel.ts`), one `QueryEngine` method (`hasRetainedReasoning()`), wires the two existing call-sites, and reorders `index.ts` boot to fire the env-warn before Ink mounts. Net delta: ~150 LOC (~80 LOC new module, ~10 LOC QueryEngine method, ~15 LOC reactLoop, ~5 LOC subagentLoop, ~10 LOC index.ts reorder, ~50 LOC integration test, plus per-file unit-test extensions). Strictly additive — R1–R12 stay intact.

**Spec:** `docs/superpowers/specs/v1.5-mycelium-design.md` §4 (HEAD `242fdd0`). Decisions are grounded there; this plan executes the spec. Round-2 inversion at §4.1.3 / §4.4 makes iter-0 unconditionally Pro.

**Branch:** `feat/phase-2-anamorph-routing` per `feedback_myceliate_workflow.md`. `--no-ff` summary merge to `main` after two-stage parallel review.

**Commit identity:** `~/Myceliate/myceliate-cli` has no git user.name/user.email set. Every `git commit` MUST be prefixed with the env-var dance from Phase 1's preamble (lines 17-21 of this file).

---

## Task 12 — `roleToModel` module + unit tests (TDD)

**Files:**
- Create: `src/runtime/roleToModel.ts`
- Create: `tests/unit/runtime/roleToModel.test.ts`

Pure-function dispatcher and its boot-time warn helper. No production callers wire to it yet — those land in T14, T15, T16. This task ships the module surface plus exhaustive unit tests so subsequent wiring has a stable target.

- [ ] **Step 1: Create the failing test file**

```ts
// tests/unit/runtime/roleToModel.test.ts
import { afterEach, describe, expect, it, vi } from 'vitest';
import {
  type SporeRole,
  checkAndWarnEnvOverride,
  roleToModel,
} from '../../../src/runtime/roleToModel.js';

const ROLES: SporeRole[] = [
  'subagent',
  'repl-execution',
  'repl-with-reasoning',
  'germination',
  'orchestrator',
];

afterEach(() => {
  vi.unstubAllEnvs();
});

describe('roleToModel — canonical mapping', () => {
  it('routes subagent + repl-execution to V4-Flash (Anamorph)', () => {
    vi.stubEnv('DEEPSEEK_MODEL', '');
    expect(roleToModel('subagent')).toBe('deepseek-v4-flash');
    expect(roleToModel('repl-execution')).toBe('deepseek-v4-flash');
  });

  it('routes repl-with-reasoning + germination + orchestrator to V4-Pro (Teleomorph)', () => {
    vi.stubEnv('DEEPSEEK_MODEL', '');
    expect(roleToModel('repl-with-reasoning')).toBe('deepseek-v4-pro');
    expect(roleToModel('germination')).toBe('deepseek-v4-pro');
    expect(roleToModel('orchestrator')).toBe('deepseek-v4-pro');
  });
});

describe('roleToModel — DEEPSEEK_MODEL env override', () => {
  it('returns env value for every role when set non-empty', () => {
    vi.stubEnv('DEEPSEEK_MODEL', 'ollama:llama3');
    for (const role of ROLES) {
      expect(roleToModel(role)).toBe('ollama:llama3');
    }
  });

  it('treats empty-string env as unset; falls back to ROLE_MAP', () => {
    vi.stubEnv('DEEPSEEK_MODEL', '');
    expect(roleToModel('subagent')).toBe('deepseek-v4-flash');
  });

  it('returns ROLE_MAP value when env undefined', () => {
    vi.unstubAllEnvs();
    delete process.env.DEEPSEEK_MODEL;
    expect(roleToModel('orchestrator')).toBe('deepseek-v4-pro');
  });
});

describe('checkAndWarnEnvOverride', () => {
  it('emits no log + no stderr when env unset', () => {
    vi.unstubAllEnvs();
    delete process.env.DEEPSEEK_MODEL;
    const logger = { warn: vi.fn(), info: vi.fn(), error: vi.fn(), debug: vi.fn() };
    const stderrSpy = vi.spyOn(process.stderr, 'write').mockImplementation(() => true);
    // biome-ignore lint/suspicious/noExplicitAny: test mock cast
    checkAndWarnEnvOverride(logger as any);
    expect(logger.warn).not.toHaveBeenCalled();
    expect(stderrSpy).not.toHaveBeenCalled();
    stderrSpy.mockRestore();
  });

  it('emits structured log + unmissable stderr when env set', () => {
    vi.stubEnv('DEEPSEEK_MODEL', 'override-model');
    const logger = { warn: vi.fn(), info: vi.fn(), error: vi.fn(), debug: vi.fn() };
    const stderrSpy = vi.spyOn(process.stderr, 'write').mockImplementation(() => true);
    // biome-ignore lint/suspicious/noExplicitAny: test mock cast
    checkAndWarnEnvOverride(logger as any);
    expect(logger.warn).toHaveBeenCalledWith({
      event: 'deepseek_model_override',
      model: 'override-model',
    });
    expect(stderrSpy).toHaveBeenCalledTimes(1);
    const stderrArg = stderrSpy.mock.calls[0]?.[0];
    expect(stderrArg).toContain('DEEPSEEK_MODEL env var is set');
    expect(stderrArg).toContain('override-model');
    expect(stderrArg).toContain('Bypassing Anamorph/Teleomorph');
    stderrSpy.mockRestore();
  });
});
```

- [ ] **Step 2: Run test → expect import failure**

Run: `pnpm test tests/unit/runtime/roleToModel.test.ts`
Expected: FAIL — module `src/runtime/roleToModel.ts` does not exist.

- [ ] **Step 3: Implement `src/runtime/roleToModel.ts` per spec §4.2**

Copy the TS surface from `docs/superpowers/specs/v1.5-mycelium-design.md` §4.2 (the fenced `// src/runtime/roleToModel.ts` block) verbatim. The spec is the source of truth; do not re-derive. The Logger import path is `../util/logger.js`.

- [ ] **Step 4: Run tests → expect green**

Run: `pnpm test tests/unit/runtime/roleToModel.test.ts`
Expected: 8 tests pass.

- [ ] **Step 5: Run full suite + biome → expect clean**

Run: `pnpm test && pnpm lint`
Expected: 509 prior tests + 8 new = 517 pass; biome lint clean (the existing single warning at `tests/unit/runtime/workerLifecycle.test.ts:259` for the non-null assertion is pre-existing and unrelated).

- [ ] **Step 6: Commit**

```bash
GIT_AUTHOR_NAME="Patrick" GIT_AUTHOR_EMAIL="amey-jonesp@cardiff.ac.uk" \
GIT_COMMITTER_NAME="Patrick" GIT_COMMITTER_EMAIL="amey-jonesp@cardiff.ac.uk" \
git commit -m "feat(routing): add roleToModel dispatcher + boot-time env-override warn"
```

---

## Task 13 — `QueryEngine.hasRetainedReasoning()` (TDD)

**Files:**
- Modify: `src/orchestrator/QueryEngine.ts`
- Modify: `tests/unit/orchestrator/QueryEngine.test.ts`

Add one method exposing R2-relevant retained-reasoning state without mutating history. Reuses existing `hasToolCalls` + `hasReasoningContent` predicates from `src/adapters/messages.ts:30-38`.

- [ ] **Step 1: Add failing test cases to `tests/unit/orchestrator/QueryEngine.test.ts`**

Append a new `describe('hasRetainedReasoning')` block with five cases (full coverage of spec §4.7 QueryEngine bullet):

1. Empty history → `false`.
2. History with only conversational assistant turns (no `tool_calls`) carrying `reasoning_content` → `false` (R2 will drop those, so they don't count as retained).
3. History with assistant turn that has `tool_calls` AND non-empty `reasoning_content` → `true`.
4. History with assistant turn that has `tool_calls` but no `reasoning_content` → `false`.
5. History with multiple turns — `true` iff ANY tool-call turn has reasoning. Construct a 4-turn history where only one mid-history assistant turn has both; assert `true`.

Use the existing `appendAssistant` / `appendToolResult` / `appendUser` API to build histories. Match the test patterns from the existing `QueryEngine.test.ts` describe blocks.

- [ ] **Step 2: Run test → expect failure**

Run: `pnpm test tests/unit/orchestrator/QueryEngine.test.ts`
Expected: FAIL — `engine.hasRetainedReasoning is not a function`.

- [ ] **Step 3: Add the method to `src/orchestrator/QueryEngine.ts`**

Insert the import at the existing `hasToolCalls` import line (currently line 4):

```ts
import { hasReasoningContent, hasToolCalls } from '../adapters/messages.js';
```

Add the method per spec §4.3 (after the existing `applyR2` method, before `prepareRequest`):

```ts
hasRetainedReasoning(): boolean {
  return this.history.some((m) => hasToolCalls(m) && hasReasoningContent(m));
}
```

- [ ] **Step 4: Run tests → expect green**

Run: `pnpm test tests/unit/orchestrator/QueryEngine.test.ts`
Expected: existing tests + 5 new cases pass.

- [ ] **Step 5: Run full suite + biome → expect clean**

Run: `pnpm test && pnpm lint`

- [ ] **Step 6: Commit**

```bash
GIT_AUTHOR_NAME="Patrick" GIT_AUTHOR_EMAIL="amey-jonesp@cardiff.ac.uk" \
GIT_COMMITTER_NAME="Patrick" GIT_COMMITTER_EMAIL="amey-jonesp@cardiff.ac.uk" \
git commit -am "feat(orchestrator): add QueryEngine.hasRetainedReasoning() for R2 ratchet"
```

---

## Task 14 — Wire `subagentLoop` to `roleToModel('subagent')` + unit test (TDD)

**Files:**
- Modify: `src/spores/subagentLoop.ts`
- Create: `tests/unit/spores/subagentLoop.test.ts`

Replace the inline `process.env.DEEPSEEK_MODEL ?? 'deepseek-reasoner'` at line 65 with `roleToModel('subagent')`. Apply the `let isError` shadow rename ride-along at line 84 (per spec §4.5 ride-along sweep). Add the missing unit test file (`ls tests/unit/spores/` confirmed it does not exist).

- [ ] **Step 1: Create the failing test file**

```ts
// tests/unit/spores/subagentLoop.test.ts
import { afterEach, describe, expect, it, vi } from 'vitest';
import type { ChatRequest, DeepSeekClient } from '../../../src/adapters/DeepSeekClient.js';
import type { StreamEvent } from '../../../src/adapters/streamEvent.js';
import { runSubagentLoop } from '../../../src/spores/subagentLoop.js';

afterEach(() => {
  vi.unstubAllEnvs();
});

function recordingClient(): { client: DeepSeekClient; captured: ChatRequest[] } {
  const captured: ChatRequest[] = [];
  const client: DeepSeekClient = {
    async *stream(req: ChatRequest): AsyncIterable<StreamEvent> {
      captured.push(req);
      // Single-turn conversational reply — no tool calls, terminate immediately.
      yield { type: 'content_delta', text: 'done' };
      yield { type: 'turn_complete' };
    },
  };
  return { client, captured };
}

describe('runSubagentLoop — Anamorph dispatch', () => {
  it('passes deepseek-v4-flash to client.stream by default', async () => {
    vi.stubEnv('DEEPSEEK_MODEL', '');
    const { client, captured } = recordingClient();
    await runSubagentLoop({
      client,
      personaSkill: 'You are a test sub-agent.',
      task: 'just say done',
      maxSteps: 1,
    });
    expect(captured).toHaveLength(1);
    expect(captured[0]?.model).toBe('deepseek-v4-flash');
  });

  it('honours DEEPSEEK_MODEL env override', async () => {
    vi.stubEnv('DEEPSEEK_MODEL', 'override-x');
    const { client, captured } = recordingClient();
    await runSubagentLoop({
      client,
      personaSkill: 'You are a test sub-agent.',
      task: 'just say done',
      maxSteps: 1,
    });
    expect(captured[0]?.model).toBe('override-x');
  });
});
```

- [ ] **Step 2: Run test → expect failure (model still resolves to old default)**

Run: `pnpm test tests/unit/spores/subagentLoop.test.ts`
Expected: FAIL — captured model is `'deepseek-reasoner'` (the V3 fallback), not `'deepseek-v4-flash'`.

- [ ] **Step 3: Edit `src/spores/subagentLoop.ts`**

Add import at the top:

```ts
import { roleToModel } from '../runtime/roleToModel.js';
```

Replace line 65 (`model: process.env.DEEPSEEK_MODEL ?? 'deepseek-reasoner',`) with:

```ts
model: roleToModel('subagent'),
```

**Ride-along — `isError` shadow rename:** at line 84 the local `let isError = false` shadows the `isError` import from `streamEvent.ts`. Rename to `let toolErrored = false` (and update the `is_error: isError` line at ~line 101 to `is_error: toolErrored`). The TODO(v1.4) marker comment at line 84 is removed. Three occurrences total inside the function body.

- [ ] **Step 4: Run tests → expect green**

Run: `pnpm test tests/unit/spores/subagentLoop.test.ts`
Expected: 2 new tests pass.

- [ ] **Step 5: Run full suite + biome → expect clean**

Run: `pnpm test && pnpm lint`
Expected: full suite green, no lint regressions.

- [ ] **Step 6: Commit**

```bash
GIT_AUTHOR_NAME="Patrick" GIT_AUTHOR_EMAIL="amey-jonesp@cardiff.ac.uk" \
GIT_COMMITTER_NAME="Patrick" GIT_COMMITTER_EMAIL="amey-jonesp@cardiff.ac.uk" \
git commit -am "feat(spores): wire subagentLoop to roleToModel('subagent') + isError rename"
```

---

## Task 15 — Wire `reactLoop` with iter-0 Pro default (TDD)

**Files:**
- Modify: `src/orchestrator/reactLoop.ts`
- Modify: `tests/unit/orchestrator/reactLoop.test.ts`

Per-iteration role dispatch with the round-2 inverted default: iteration 0 always Pro, subsequent iterations ratchet on `engine.hasRetainedReasoning()`. `opts.model` becomes optional explicit override (test-fixture path).

- [ ] **Step 1: Add failing test cases to `tests/unit/orchestrator/reactLoop.test.ts`**

Append a new `describe('reactLoop — Anamorph routing dispatch')` block covering the four spec §4.7 reactLoop bullets:

1. **Fresh engine + iteration 0 → Pro.** Mount `runReactLoop` against a recording-adapter that on the first call yields one assistant message with no tool calls (terminating the loop after iter 0). Assert the captured `request.model` is `'deepseek-v4-pro'`.

2. **Iteration 1 with no retained reasoning → Flash demotion.** Recording adapter yields, on the first call, one assistant message with a tool call but NO `reasoning_content`. The loop runs the tool (use a stub tool that returns immediately), then issues a second `client.stream` call. Assert: iter 0 captured `'deepseek-v4-pro'`, iter 1 captured `'deepseek-v4-flash'`.

3. **Reasoning-retained ratchet stays Pro.** Recording adapter yields, on the first call, one assistant message with a tool call AND non-empty `reasoning_content`. Iter 1 should capture `'deepseek-v4-pro'`. Iter 2 (with the ratchet still engaged) captures `'deepseek-v4-pro'`.

4. **`DEEPSEEK_MODEL=test-override` env wins for every iteration.** With env set, both iter 0 and iter 1 capture `'test-override'` regardless of engine state.

5. **Explicit `opts.model: 'mock'` supplied → both iterations capture `'mock'`.** (Legacy test-fixture override path still works.)

Match the existing reactLoop.test.ts setup pattern for the recording adapter and stub `ToolRegistry`.

- [ ] **Step 2: Run test → expect failures**

Run: `pnpm test tests/unit/orchestrator/reactLoop.test.ts`
Expected: 5 new cases fail (existing `opts.model` is `undefined` → `request.model` is `undefined`, since current code passes `opts.model` straight through).

- [ ] **Step 3: Edit `src/orchestrator/reactLoop.ts`**

Add the import:

```ts
import { roleToModel, type SporeRole } from '../runtime/roleToModel.js';
```

Make `model` optional in `ReactLoopOptions`:

```ts
export type ReactLoopOptions = {
  client: DeepSeekClient;
  engine: QueryEngine;
  tools: ToolRegistry;
  /** Optional explicit override; when unset, role-based dispatch fires per iter. */
  model?: string;
  // ... rest unchanged
};
```

Replace the body of the `for (let iter ...)` loop's first statement per spec §4.4:

```ts
for (let iter = 0; iter < maxIters; iter++) {
  // Iteration 0 always Pro (planning bias — see spec §4.1.3); subsequent
  // iterations ratchet on retained reasoning_content per R2. Once Pro,
  // stays Pro within the session (monotonic rule).
  const role: SporeRole =
    iter === 0 || opts.engine.hasRetainedReasoning()
      ? 'repl-with-reasoning'
      : 'repl-execution';
  const model = opts.model ?? roleToModel(role);

  const request = opts.engine.prepareRequest({
    model,
    tools: opts.tools.definitions(),
    thinking: true,
    strict: true,
    ...(opts.signal ? { signal: opts.signal } : {}),
  });
  // ... (rest of loop body unchanged)
}
```

- [ ] **Step 4: Run tests → expect green**

Run: `pnpm test tests/unit/orchestrator/reactLoop.test.ts`
Expected: existing tests + 5 new cases pass. Existing tests that supplied `model: 'something'` continue to work via the optional override path.

- [ ] **Step 5: Run full suite + biome → expect clean**

Run: `pnpm test && pnpm lint`

- [ ] **Step 6: Commit**

```bash
GIT_AUTHOR_NAME="Patrick" GIT_AUTHOR_EMAIL="amey-jonesp@cardiff.ac.uk" \
GIT_COMMITTER_NAME="Patrick" GIT_COMMITTER_EMAIL="amey-jonesp@cardiff.ac.uk" \
git commit -am "feat(orchestrator): per-iteration role dispatch with iter-0 Pro default"
```

---

## Task 16 — Boot-time env warn at `index.ts` + replSession cleanup

**Files:**
- Modify: `src/index.ts`
- Modify: `src/runtime/replSession.ts`
- Modify: `tests/unit/runtime/replSession.test.ts` (only if existing tests break — likely none do)

Reorder boot so `checkAndWarnEnvOverride` fires before Ink mounts (writes to stderr safely per U4). Make `ReplSessionOptions.model` optional and stop threading it from production callers. The `index.ts:52` direct read of `DEEPSEEK_MODEL` for onboarding-banner seeding stays — it is read-only banner state, not a routing decision.

- [ ] **Step 1: Reorder `src/index.ts` boot sequence**

Today's order (lines 47-63):
```
runOnboarding(...)        // reads DEEPSEEK_MODEL for banner seed
parseResumeFlag / parseNoSporeFlag
senseContext(...)
sessionId
createLogger(...)
```

New order — move `senseContext` + `createLogger` + new `checkAndWarnEnvOverride` ahead of `runOnboarding`:

```ts
// near top of main()
const ctx = await senseContext({ cwd: process.cwd() });
const logger = createLogger({ logsDir: join(ctx.memoryDir, 'logs') });
checkAndWarnEnvOverride(logger);

const onboarding = await runOnboarding({
  ...(process.env.DEEPSEEK_API_KEY ? { apiKey: process.env.DEEPSEEK_API_KEY } : {}),
  ...(process.env.DEEPSEEK_ADAPTER === 'v3' || process.env.DEEPSEEK_ADAPTER === 'v4'
    ? { adapter: process.env.DEEPSEEK_ADAPTER }
    : {}),
  ...(process.env.DEEPSEEK_MODEL ? { model: process.env.DEEPSEEK_MODEL } : {}),
});
```

(`memory`, `conversation`, and the resume-flag handling that depend on `ctx` / `sessionId` / `logger` follow as before. The reorder is safe — `senseContext` and `createLogger` are pure / fs-only.)

Add the import:

```ts
import { checkAndWarnEnvOverride } from './runtime/roleToModel.js';
```

- [ ] **Step 2: Stop passing `model` from `replSession` to `runReactLoop` in production paths**

In `src/index.ts` where `runReplSession({ model: onboarding.model, ... })` is called (around line 250+ — locate via `grep -n "runReplSession" src/index.ts`): drop the `model:` field from the options object. Onboarding's `model` field is now banner-display only.

In `src/runtime/replSession.ts` (lines 18-62): make `model` optional:

```ts
export type ReplSessionOptions = {
  client: DeepSeekClient;
  tools: ToolRegistry;
  /** Optional explicit override; when unset, runReactLoop's role-based dispatch fires. */
  model?: string;
  cwd: string;
  // ... rest unchanged
};
```

Inside `runReplSession` body (lines 112-118 and 173-179), change both `model: opts.model,` lines in the `runReactLoop` calls to `...(opts.model ? { model: opts.model } : {}),` — conditional spread so the field is absent when not provided (matches `exactOptionalPropertyTypes` discipline used elsewhere in the file).

- [ ] **Step 3: Banner display tweak in `src/index.ts`**

Where the banner is constructed (around line 135-139):

```ts
const banner = {
  model: onboarding.model,
  adapter: onboarding.adapter,
  cwd: process.cwd(),
};
```

Update to display "auto" when no DEEPSEEK_MODEL override:

```ts
const banner = {
  model: process.env.DEEPSEEK_MODEL && process.env.DEEPSEEK_MODEL.length > 0
    ? process.env.DEEPSEEK_MODEL
    : 'auto (Anamorph/Teleomorph)',
  adapter: onboarding.adapter,
  cwd: process.cwd(),
};
```

- [ ] **Step 4: Run full suite + biome**

Run: `pnpm test && pnpm lint`
Expected: all tests still pass. If `tests/unit/runtime/replSession.test.ts` supplies `model:` in its setup, those tests continue to work because the field is now optional. If they break, update the failing tests to either drop the field or keep it as explicit override (test-fixture path).

- [ ] **Step 5: Commit**

```bash
GIT_AUTHOR_NAME="Patrick" GIT_AUTHOR_EMAIL="amey-jonesp@cardiff.ac.uk" \
GIT_COMMITTER_NAME="Patrick" GIT_COMMITTER_EMAIL="amey-jonesp@cardiff.ac.uk" \
git commit -am "feat(boot): reorder for env-warn + drop session-level model from production path"
```

---

## Task 17 — Integration test: anamorph routing reaches the wire

**Files:**
- Create: `tests/integration/anamorphRouting.test.ts`

The "plumbing-reaches-the-wire" assertion Gemini explicitly required. Mounts the full subagent and orchestrator paths against a recording adapter; confirms that the model field on each `client.stream(request)` call carries the correct routed value. No Redis required — recording adapter is a pure async-iterable stub.

- [ ] **Step 1: Create the test file**

```ts
// tests/integration/anamorphRouting.test.ts
import { afterEach, describe, expect, it, vi } from 'vitest';
import type { ChatRequest, DeepSeekClient } from '../../src/adapters/DeepSeekClient.js';
import type { StreamEvent } from '../../src/adapters/streamEvent.js';
import { QueryEngine } from '../../src/orchestrator/QueryEngine.js';
import { runReactLoop } from '../../src/orchestrator/reactLoop.js';
import { ToolRegistry } from '../../src/tools/registry.js';
import { runSubagentLoop } from '../../src/spores/subagentLoop.js';

afterEach(() => {
  vi.unstubAllEnvs();
});

describe('anamorph routing — plumbing reaches the wire', () => {
  it('subagentLoop dispatches deepseek-v4-flash', async () => {
    vi.stubEnv('DEEPSEEK_MODEL', '');
    const captured: ChatRequest[] = [];
    const client: DeepSeekClient = {
      async *stream(req): AsyncIterable<StreamEvent> {
        captured.push(req);
        yield { type: 'content_delta', text: 'ack' };
        yield { type: 'turn_complete' };
      },
    };
    await runSubagentLoop({
      client,
      personaSkill: 'test sub-agent',
      task: 'noop',
      maxSteps: 1,
    });
    expect(captured.at(0)?.model).toBe('deepseek-v4-flash');
  });

  it('reactLoop iteration 0 dispatches deepseek-v4-pro (planning bias)', async () => {
    vi.stubEnv('DEEPSEEK_MODEL', '');
    const captured: ChatRequest[] = [];
    const client: DeepSeekClient = {
      async *stream(req): AsyncIterable<StreamEvent> {
        captured.push(req);
        yield { type: 'content_delta', text: 'ack' };
        yield { type: 'turn_complete' };
      },
    };
    const engine = new QueryEngine({ systemPrompt: 'sys', workingBudget: 200_000 });
    engine.appendUser('what is 2+2');
    const tools = new ToolRegistry();
    for await (const _ev of runReactLoop({ client, engine, tools, maxIterations: 1 })) {
      // drain
    }
    expect(captured.at(0)?.model).toBe('deepseek-v4-pro');
  });

  it('reactLoop demotes to flash on iteration 1 when iter 0 produced no retained reasoning', async () => {
    vi.stubEnv('DEEPSEEK_MODEL', '');
    let call = 0;
    const captured: ChatRequest[] = [];
    const client: DeepSeekClient = {
      async *stream(req): AsyncIterable<StreamEvent> {
        captured.push(req);
        call += 1;
        if (call === 1) {
          // Iter 0: produce a tool call WITHOUT reasoning_content. Reasoning ratchet
          // should NOT engage (hasRetainedReasoning checks for tool_calls AND reasoning).
          yield { type: 'tool_call', id: 't1', name: 'noop', args: {} };
          yield { type: 'turn_complete' };
        } else {
          yield { type: 'content_delta', text: 'done' };
          yield { type: 'turn_complete' };
        }
      },
    };
    const engine = new QueryEngine({ systemPrompt: 'sys', workingBudget: 200_000 });
    engine.appendUser('run noop');
    const tools = new ToolRegistry();
    tools.register({
      name: 'noop',
      description: 'no-op tool for testing',
      // biome-ignore lint/suspicious/noExplicitAny: stub tool
      inputSchema: { parse: (x: unknown) => x } as any,
      handler: async () => 'noop done',
    });
    for await (const _ev of runReactLoop({ client, engine, tools, maxIterations: 2 })) {
      // drain
    }
    expect(captured.at(0)?.model).toBe('deepseek-v4-pro');     // iter 0 — planning bias
    expect(captured.at(1)?.model).toBe('deepseek-v4-flash');   // iter 1 — demoted
  });

  it('reactLoop ratchets to Pro after retained reasoning', async () => {
    vi.stubEnv('DEEPSEEK_MODEL', '');
    let call = 0;
    const captured: ChatRequest[] = [];
    const client: DeepSeekClient = {
      async *stream(req): AsyncIterable<StreamEvent> {
        captured.push(req);
        call += 1;
        if (call === 1) {
          yield { type: 'reasoning_delta', text: 'thinking deeply' };
          yield { type: 'tool_call', id: 't1', name: 'noop', args: {} };
          yield { type: 'turn_complete' };
        } else {
          yield { type: 'content_delta', text: 'done' };
          yield { type: 'turn_complete' };
        }
      },
    };
    const engine = new QueryEngine({ systemPrompt: 'sys', workingBudget: 200_000 });
    engine.appendUser('think hard then run noop');
    const tools = new ToolRegistry();
    tools.register({
      name: 'noop',
      description: 'no-op tool for testing',
      // biome-ignore lint/suspicious/noExplicitAny: stub tool
      inputSchema: { parse: (x: unknown) => x } as any,
      handler: async () => 'noop done',
    });
    for await (const _ev of runReactLoop({ client, engine, tools, maxIterations: 2 })) {
      // drain
    }
    expect(captured.at(0)?.model).toBe('deepseek-v4-pro');   // iter 0 — planning bias
    expect(captured.at(1)?.model).toBe('deepseek-v4-pro');   // iter 1 — ratchet
  });
});
```

(If the `ToolRegistry.register` shape differs from this stub, adapt — read `src/tools/registry.ts` for the canonical Tool type. The intent is "register a no-op tool that the engine can resolve so iter 1 actually fires".)

- [ ] **Step 2: Run integration test → expect green**

Run: `pnpm test tests/integration/anamorphRouting.test.ts`
Expected: 4 tests pass.

- [ ] **Step 3: Run full suite + biome → expect clean**

Run: `pnpm test && pnpm lint`

- [ ] **Step 4: Commit**

```bash
GIT_AUTHOR_NAME="Patrick" GIT_AUTHOR_EMAIL="amey-jonesp@cardiff.ac.uk" \
GIT_COMMITTER_NAME="Patrick" GIT_COMMITTER_EMAIL="amey-jonesp@cardiff.ac.uk" \
git commit -am "test(integration): anamorph routing reaches the wire (subagent + reactLoop)"
```

---

## Task 18 — Doc and config updates

**Files:**
- Modify: `README.md`
- Modify: `docs/MANUAL_SMOKE.md`
- Modify (or check exists): `.env.example`

Per spec §4.8.

- [ ] **Step 1: README architecture paragraph**

In `README.md`'s architecture / "How it works" section, add a paragraph:

> **Model routing.** Model selection is automatic per call — Anamorph (V4-Flash) for mechanical/subagent loops, Teleomorph (V4-Pro) for strategic and reasoning-active turns. Iteration 0 of every orchestrator REPL turn defaults to Pro (planning bias), then ratchets to Flash on subsequent iterations if no retained reasoning is present. Set `DEEPSEEK_MODEL=<name>` to override globally; an unmissable warn line confirms the bypass at boot.

- [ ] **Step 2: MANUAL_SMOKE walk-point 9**

Append to `docs/MANUAL_SMOKE.md`:

```markdown
## Walk-point 9 — Model routing (Phase 2)

After running any multi-turn orchestrator task, `tail .myceliate/logs/session.log` should show:

- Subagent dispatches always log `model: 'deepseek-v4-flash'`.
- Iteration 0 of any orchestrator REPL turn always logs `model: 'deepseek-v4-pro'` (planning bias).
- Subsequent iterations log Pro if any prior tool-call assistant turn carried `reasoning_content`, Flash otherwise.

To verify the env-override warn: `DEEPSEEK_MODEL=test-override myceliate` should write a single `[myceliate] DEEPSEEK_MODEL env var is set...` line to stderr before Ink mounts, then route every call to `'test-override'`.
```

- [ ] **Step 3: `.env.example`**

Check whether `.env.example` exists at repo root. If not, create it minimally with the documented vars; if yes, append:

```bash
# DEEPSEEK_MODEL=deepseek-v4-pro  # optional override — bypasses Anamorph/Teleomorph routing
```

- [ ] **Step 4: Commit**

```bash
GIT_AUTHOR_NAME="Patrick" GIT_AUTHOR_EMAIL="amey-jonesp@cardiff.ac.uk" \
GIT_COMMITTER_NAME="Patrick" GIT_COMMITTER_EMAIL="amey-jonesp@cardiff.ac.uk" \
git commit -am "docs(routing): README + MANUAL_SMOKE walk-point 9 + .env.example for Phase 2"
```

---

## Task 19 — Manual smoke regression (verification only, no commit)

Per spec §4.7 smoke regression. Re-run v1.4 walk-points 2-8 with role routing live. **No code changes, no commit** — this is a final verification before opening the review round.

- [ ] **Step 1: Boot myceliate cleanly**

```bash
pnpm build
docker compose up -d redis
unset DEEPSEEK_MODEL  # ensure no override
pnpm queue:worker &  # or rely on Phase 1's auto-spawn
myceliate
```

Confirm onboarding banner shows "Model: auto (Anamorph/Teleomorph)" (or your chosen banner text from T16 step 3). No stderr `[myceliate] DEEPSEEK_MODEL` warn line.

- [ ] **Step 2: Run a multi-turn task that exercises both paths**

In the REPL, type a planning prompt that triggers reasoning then tool calls. Example:

```
Plan and execute: read README.md, summarise its sections, list every file in src/runtime/.
```

- [ ] **Step 3: `tail` the agent log and confirm routing**

```bash
tail -50 .myceliate/logs/session.log | grep request_started
```

Expected pattern:
- Iter 0 of orchestrator REPL turn → `model: 'deepseek-v4-pro'`.
- If model produced reasoning + tool calls on iter 0 → iter 1+ also `'deepseek-v4-pro'` (ratchet).
- If iter 0 was a single mechanical turn (no reasoning) → iter 1 `'deepseek-v4-flash'`.
- Any subagent dispatch → `'deepseek-v4-flash'`.

- [ ] **Step 4: Verify env override warn works**

```bash
DEEPSEEK_MODEL=test-override myceliate 2>&1 | head -5
```

Expected: stderr line `[myceliate] DEEPSEEK_MODEL env var is set. Bypassing Anamorph/Teleomorph role routing and forcing model: test-override` BEFORE the Ink banner. Banner shows "Model: test-override".

- [ ] **Step 5: Walk-points 2-8 regression**

Quickly re-run v1.4 walk-points 2-8 (file edits, bash dispatch, HITL approval, spore germination, etc.). Each should still work — Phase 2 is strictly additive, no behavioural regression expected. Note any oddities.

If everything passes: Phase 2 implementation is complete and ready for two-stage parallel review (general-purpose × 2, sonnet — spec compliance + code quality, per `feedback_myceliate_workflow.md`).

---

## Phase 3 — Exoenzyme

**Goal:** External MCP server schema → lightweight Markdown skill translation per Vector 6 of the strategic research brief. **One MCP server → one spore** (§5.1.1). **Multi-active model** (§5.1.6): tool wrappers from any germinated MCP-spore stay registered until explicit teardown; only the most-recently-germinated spore's body lives in the system prompt. Lazy server spawn on germination via `McpLifecycle`. Sensitive operations route through Cortina HitlGate's new `kind:'mcp'` `ApprovalRequest` variant. Target servers: Playwright (browser automation) + Postgres (DB introspection) as proof-of-concept.

**Architecture:** Five new modules (`src/mcp/{McpClient,SchemaTranslator}.ts`, `src/runtime/mcpLifecycle.ts`, `src/cli/{parseSubcommand,mcpInstall}.ts`), manifest extension (`mcp_server` optional field), `ToolRegistry.deregister`/`deregisterByPrefix` + dual-schema `Tool<Input>`, `HitlGate` discriminated union refactor (`bash|write|read|mcp`), wire-touches in `src/tools/germinate_spore.ts`, `src/runtime/bootTools.ts`, `src/runtime/replSession.ts`, `src/index.ts`, `src/ui/{ApprovalPrompt.tsx,App.tsx}`. Net delta ~700-900 LOC production + ~700-900 LOC tests.

**Spec:** `docs/superpowers/specs/v1.5-mycelium-design.md` §5 (HEAD `baf4bd8`, post-Gemini+subagent fix-pass). The §5.7.4 wire-touch site map enumerates every file Phase 3 touches. Spec is the source of truth; this plan executes it task-by-task.

**Branch:** `feat/phase-3-exoenzyme` per `feedback_myceliate_workflow.md`. `--no-ff` summary merge to `main` after two-stage parallel review.

**Commit identity:** `~/Myceliate/myceliate-cli` has no git user.name/user.email set. Every `git commit` MUST be prefixed with the env-var dance from Phase 1's preamble (lines 17-21 of this file).

**Lessons forwarded from Phase 1+2 implementer briefs:**

- **Verify against codebase before specifying changes** (`feedback_verify_codebase_first.md`) — Phase 2 caught `germinate_spore.ts` was NOT a wire-touch via this discipline, and the Phase 3 fix-pass caught `bootTools.ts`/`ApprovalPrompt.tsx`/`App.tsx`/`/spore unpin` path/finally block as missed wire-touches. Live AST is ground truth.
- **Recording-adapter test pattern** (Phase 2 T17) — capture wire calls from production code paths against a recording adapter, NOT mocks injecting expected values. T22 + T30 transfer this pattern to MCP via `RecordingMcpClient` + the fake-server fixture.
- **Per-task TDD commits** (failing test → minimum implementation → passing test → commit). One review-fix commit per round at the end. Phase 2 had 8 implementer commits + 1 review-fix; Phase 3 expects 12 implementer commits (T20–T31) + 1 review-fix.
- **Spec is the source of truth** — when the task says "implement per spec §X.Y", copy the TS surface verbatim from the spec. Do not re-derive from the task description; the task description is a roadmap, the spec is the contract.

---

## Task 20 — `SporeManifest` `mcp_server` extension (TDD)

**Files:**

- Modify: `src/spores/SporeManifest.ts`
- Modify: `tests/unit/spores/SporeManifest.test.ts`

Foundation task — adds the optional `mcp_server` field that downstream Phase 3 modules consume. Per spec §5.2: the field MUST be added INSIDE the existing `.object({...})` literal, NOT via `.extend()` (which silently drops `.strict()` — see §5.2 for the trap).

- [ ] **Step 1: Add failing test cases to `tests/unit/spores/SporeManifest.test.ts`**

Append a `describe('mcp_server field')` block:

- `mcp_server` absent → manifest validates as before (regression coverage; existing manifests must not break).
- `mcp_server` present with valid `command`/`args`/`env`/`sensitive_tools` → validates; `args`/`env`/`sensitive_tools` default to `[]`/`{}`/`[]`; `call_timeout_ms` is optional.
- `mcp_server.command` empty string → fails validation (`must be at least 1 character`).
- `mcp_server.sensitive_tools` containing a non-string → fails validation.
- `mcp_server.call_timeout_ms` zero or negative or non-integer → fails (must be positive int).
- A manifest with an unknown top-level key still fails (`.strict()` preserved — explicit regression for the trap).

- [ ] **Step 2: Run test → expect failures**

Run: `pnpm test tests/unit/spores/SporeManifest.test.ts`
Expected: new tests fail (the field doesn't exist yet); existing tests still pass.

- [ ] **Step 3: Implement per spec §5.2**

Edit `src/spores/SporeManifest.ts`. Insert the `mcp_server` field INSIDE the existing `SporeManifestSchema.object({...})` literal — copy the TS surface from spec §5.2 verbatim.

- [ ] **Step 4: Run tests → expect green**

Run: `pnpm test tests/unit/spores/SporeManifest.test.ts`

- [ ] **Step 5: Run full suite + biome → expect clean**

Run: `pnpm test && pnpm lint`
Expected: 532 prior tests + N new pass; biome clean (single pre-existing warning at `tests/unit/runtime/workerLifecycle.test.ts:259` is OK).

- [ ] **Step 6: Commit**

```bash
GIT_AUTHOR_NAME="Patrick" GIT_AUTHOR_EMAIL="amey-jonesp@cardiff.ac.uk" \
GIT_COMMITTER_NAME="Patrick" GIT_COMMITTER_EMAIL="amey-jonesp@cardiff.ac.uk" \
git commit -m "feat(spores): add optional mcp_server field to SporeManifest"
```

---

## Task 21 — `ToolRegistry` deregister + dual-schema `Tool<Input>` (TDD)

**Files:**

- Modify: `src/tools/registry.ts`
- Modify: `src/tools/{bash,readFile,writeFile,grep,listDir}.ts` — wrap existing zod schemas in `{ kind: 'zod', zod: ... }` discriminator at each `register(...)` call site. Mechanical refactor.
- Modify: `src/runtime/bootTools.ts` — wrap stub bash schema in discriminator (line 76 area), plus the `germinate_spore` and `spawn_subagent` wrapper registrations.
- Create: `tests/unit/tools/registry.test.ts` (or extend if a registry test file exists).

Adds two extensions per spec §5.7.1: (a) `deregister(name)` + `deregisterByPrefix(prefix)` for spore teardown; (b) `Tool<Input>.inputSchema` becomes a discriminated union `{ kind: 'zod' | 'json-schema' }` so MCP tool wrappers (T28) can register raw JSON Schemas without round-tripping through Zod (the `jsonSchemaToZod.ts` converter the original draft proposed is gone — Gemini #2).

- [ ] **Step 1: Add failing tests to `tests/unit/tools/registry.test.ts`**

Per spec §5.9 ToolRegistry bullet:

- `deregister(name)` removes the named tool; subsequent `getActiveTools` excludes it.
- `deregister(name)` is a no-op for unknown names (no throw).
- `deregisterByPrefix(prefix)` removes all matching; returns count.
- `Tool<Input>` with `inputSchema: { kind: 'zod', zod: ... }` validates input via zod; `definitions()` calls `zodToStrictJsonSchema`.
- `Tool<Input>` with `inputSchema: { kind: 'json-schema', jsonSchema: ... }` passes input through unchanged to `run()`; `definitions()` returns the raw JSON Schema verbatim.
- Mixed registry (zod + json-schema) — `definitions()` returns both shapes correctly.
- Allowlist filter works with both schema kinds.
- Re-registering after `deregister` works (no "already registered" throw).
- Registering a duplicate name without prior `deregister` still throws (regression).

- [ ] **Step 2: Run test → expect failures**

Methods don't exist; `Tool<Input>` type doesn't compile against new shape (the existing wrappers in bash.ts etc. need updating in step 3).

- [ ] **Step 3: Implement per spec §5.7.1**

Edit `src/tools/registry.ts`:

- Change `Tool<Input>.inputSchema` to discriminated union per spec §5.7.1(b).
- Add `deregister(name): void` and `deregisterByPrefix(prefix): number` methods per spec §5.7.1(a).
- `definitions()` and `invoke()` switch on `inputSchema.kind`.

Then update every existing `tools.register({...})` call site (`bash.ts`, `readFile.ts`, `writeFile.ts`, `grep.ts`, `listDir.ts`, plus the wrapping registrations in `bootTools.ts` for `germinate_spore`/`spawn_subagent`/bash-stub) to wrap the schema as `{ kind: 'zod', zod: existingSchema }`. No behaviour change — zod-kind tools behave identically to before.

- [ ] **Step 4: Run tests → expect green**

All Phase 1+1.5+2 regression tests must still pass — the dual-schema change is additive.

- [ ] **Step 5: Run full suite + biome + typecheck → expect clean**

- [ ] **Step 6: Commit**

```bash
GIT_AUTHOR_NAME="Patrick" GIT_AUTHOR_EMAIL="amey-jonesp@cardiff.ac.uk" \
GIT_COMMITTER_NAME="Patrick" GIT_COMMITTER_EMAIL="amey-jonesp@cardiff.ac.uk" \
git commit -m "feat(tools): ToolRegistry deregister + dual-schema Tool<Input>"
```

---

## Task 22 — `McpClient` SDK wrapper + unit tests (TDD)

**Files:**

- Create: `src/mcp/McpClient.ts`
- Create: `tests/unit/mcp/McpClient.test.ts`
- Create: `tests/fixtures/mcp/fake-server.mjs` (small node script speaking JSON-RPC over stdio)
- Modify: `package.json` — add `@modelcontextprotocol/sdk` to `dependencies`.

Wire-format wrapper around the official MCP SDK's stdio client. Per spec §5.3: thin wrapper with stable internal interface (independent of SDK churn), `RecordingMcpClient` test seam (T30), single-chokepoint property for the v1.6 negative-agent middleware. Adds hang protection via `callTimeoutMs` (default 30s) and faulted-state-on-crash semantics with custom error classes.

- [ ] **Step 1: Add `@modelcontextprotocol/sdk` dependency**

Run: `pnpm add @modelcontextprotocol/sdk` (pin to a specific minor version; SDK 1.x ship may require revisit). Audit transitive deps before commit (zod re-pin / json-schema validator may be added).

- [ ] **Step 2: Create `tests/fixtures/mcp/fake-server.mjs`**

Small Node script speaking JSON-RPC over stdio. Responds to:

- `initialize` (returns canned `protocolVersion`, `serverInfo`).
- `tools/list` (returns 2-3 sample tools with declared `inputSchema` JSON Schemas — including one named `echo` for round-trip tests).
- `tools/call` (echoes args back as canned result content).

Configurable via env vars: `FAKE_INITIALIZE_DELAY_MS`, `FAKE_CALL_DELAY_MS`, `FAKE_TRAP_SIGTERM=1` (for SIGKILL escalation tests in T24), `FAKE_DEBUG_TO_STDOUT=1` (for stdio-hygiene reproduction).

- [ ] **Step 3: Add failing tests to `tests/unit/mcp/McpClient.test.ts`**

Cases per spec §5.9 McpClient.test.ts bullet list:

- `initialize` resolves with the fake's `protocolVersion`.
- `listTools` returns the fake's declared tools.
- `callTool('echo', { x: 1 })` round-trips args + returns canned result.
- `close` cleanly shuts down the fake.
- `initialize` rejects on timeout (use `initializeTimeoutMs: 100` against `FAKE_INITIALIZE_DELAY_MS=1000`).
- `callTool` rejects with `McpToolTimeoutError` when the fake takes longer than `callTimeoutMs` (use `callTimeoutMs: 100` against `FAKE_CALL_DELAY_MS=1000`).
- Faulted state: kill the fake mid-session (without `client.close()`) → subsequent `callTool` rejects immediately with `McpServerCrashedError`; `isFaulted()` returns true.
- `onUnexpectedExit` handler fires exactly once with code/signal info on non-clean exit; does NOT fire when caller invokes `client.close()` (clean shutdown).

- [ ] **Step 4: Run test → expect failures (module not found)**

- [ ] **Step 5: Implement `src/mcp/McpClient.ts` per spec §5.3**

Copy the TS surface from spec §5.3 verbatim — `McpToolDescriptor`, `McpToolResult`, `McpClient` interface, `McpServerCrashedError` class, `McpToolTimeoutError` class, `McpClientOpts` interface, `createMcpClient` factory.

Internal implementation:

- Use SDK's `Client` + `StdioClientTransport` (`@modelcontextprotocol/sdk/client/index.js`, `.../client/stdio.js`).
- Inline comment about stdio non-JSON debug output gotcha (per spec §5.3 last paragraph).
- `callTool` uses `Promise.race([sdk.callTool(...), timeoutPromise])` where the timeout rejects with `McpToolTimeoutError`.
- Faulted state: subscribe to transport `'close'` event; on non-clean exit set internal `_faulted = true` + invoke any registered `onUnexpectedExit` listener.
- Route child stderr to `.myceliate/logs/mcp-<server>.log` (the file is created/appended here OR by `mcpLifecycle` in T24 — implementer's call but document the split inline).

- [ ] **Step 6: Run tests → expect green**

- [ ] **Step 7: Run full suite + biome + typecheck → expect clean**

- [ ] **Step 8: Commit**

```bash
GIT_AUTHOR_NAME="Patrick" GIT_AUTHOR_EMAIL="amey-jonesp@cardiff.ac.uk" \
GIT_COMMITTER_NAME="Patrick" GIT_COMMITTER_EMAIL="amey-jonesp@cardiff.ac.uk" \
git commit -m "feat(mcp): McpClient SDK wrapper with timeout + crash semantics + custom errors"
```

---

## Task 23 — `SchemaTranslator` pure function (TDD)

**Files:**

- Create: `src/mcp/SchemaTranslator.ts`
- Create: `tests/unit/mcp/SchemaTranslator.test.ts`
- Create: `tests/fixtures/mcp/single-tool/{tools.json, manifest.yaml, expected-skill.md, expected-commands/}` (snapshot fixtures).
- Create: `tests/fixtures/mcp/multi-tool-sensitive/...` (multi-tool + sensitive case fixtures).

Pure function — no I/O, no state. Given `McpToolDescriptor[]` + manifest, emits the SKILL.md body and per-tool `commands/*.md` content. Per spec §5.4: filename uses raw tool name (matches existing `parseSkillFrontmatter` validation that basename matches `name`); body call references use namespaced form.

- [ ] **Step 1: Create snapshot fixtures**

`tests/fixtures/mcp/single-tool/`:

- `tools.json` — array of 1 `McpToolDescriptor` (`{ name: 'fetch', description: '...', inputSchema: { type: 'object', properties: { url: { type: 'string' } }, required: ['url'] } }`).
- `manifest.yaml` — minimal manifest (`name: fetcher`, `description: ...`, `mcp_server: { command: 'npx', args: ['fake'], sensitive_tools: [] }`).
- `expected-skill.md` — checked-in golden output of `translateMcpSchema(tools, manifest).skillBody` (manually verify it includes the auto-gen marker per §5.1.2).
- `expected-commands/fetch.md` — golden command file content.

`tests/fixtures/mcp/multi-tool-sensitive/`: 3 tools, one declared sensitive in the manifest. Golden output should include the "Sensitive operations" section.

- [ ] **Step 2: Add failing tests to `tests/unit/mcp/SchemaTranslator.test.ts`**

Per spec §5.9 SchemaTranslator.test.ts bullet list:

- Single tool with primitive args → snapshot match against `tests/fixtures/mcp/single-tool/expected-skill.md` and `expected-commands/fetch.md`.
- Multiple tools, some declared sensitive → "Sensitive operations" section appears in SKILL.md; per-command frontmatter flag set.
- Tool with no args → renders without arg section.
- Tool with nested object args → renders args as bulleted hierarchy.
- Empty tools list → emits a "no capabilities discovered" SKILL.md + logger warn.
- Generated SKILL.md body ends with the auto-gen marker (per §5.1.2).

- [ ] **Step 3: Run test → expect failures**

- [ ] **Step 4: Implement `src/mcp/SchemaTranslator.ts` per spec §5.4**

Pure function `translateMcpSchema(tools, manifest): TranslationResult`. Renders SKILL.md template per spec §5.4 ("Generated SKILL.md structure" block) + `commands/*.md` template per tool. Auto-gen marker per §5.1.2.

- [ ] **Step 5: Run tests → expect green**

- [ ] **Step 6: Run full suite + biome + typecheck → expect clean**

- [ ] **Step 7: Commit**

```bash
GIT_AUTHOR_NAME="Patrick" GIT_AUTHOR_EMAIL="amey-jonesp@cardiff.ac.uk" \
GIT_COMMITTER_NAME="Patrick" GIT_COMMITTER_EMAIL="amey-jonesp@cardiff.ac.uk" \
git commit -m "feat(mcp): SchemaTranslator pure function for tools/list -> SKILL.md + commands"
```

---

## Task 24 — `McpLifecycle` module (TDD)

**Files:**

- Create: `src/runtime/mcpLifecycle.ts`
- Create: `tests/unit/runtime/mcpLifecycle.test.ts`

Sibling to `workerLifecycle.ts`. Owns the lifecycle of MCP server child processes — multiple alive concurrently per spec §5.1.6 multi-active model. Per spec §5.5: idempotent spawn, child-exit listener bridged to `onUnexpectedExit` callback, SIGTERM/SIGKILL escalation matching Phase 1 patterns.

- [ ] **Step 1: Add failing tests to `tests/unit/runtime/mcpLifecycle.test.ts`**

Cases per spec §5.9 mcpLifecycle.test.ts bullet list (uses `fake-server.mjs` from T22):

- `spawn(spore)` resolves with working client; `getActive(spore.name)` returns same instance.
- `spawn` rejects when `mcp_server` absent from manifest.
- `teardown` SIGTERMs cleanly; fake exits within grace.
- `teardown` escalates to SIGKILL on `FAKE_TRAP_SIGTERM=1`.
- `teardownAll` shuts down multiple concurrent spores.
- Stdio routing: `.myceliate/logs/mcp-<server>.log` exists, contains fake's stderr.
- Multi-active: `spawn(playwrightSpore)` + `spawn(postgresSpore)` yields 2 distinct clients; `listActive()` returns both.
- Idempotency: `spawn(playwrightSpore)` twice returns the SAME client; one child process only.
- `onUnexpectedExit` bridge: kill fake mid-session → `opts.onUnexpectedExit` fires with name + exit info; entry removed from active map.
- Explicit teardown does NOT trigger `onUnexpectedExit`.
- Per-spore `call_timeout_ms` override in manifest takes precedence over lifecycle default.

- [ ] **Step 2: Run test → expect failures (module not found)**

- [ ] **Step 3: Implement `src/runtime/mcpLifecycle.ts` per spec §5.5**

Copy the TS surface verbatim — `McpLifecycleOpts` interface, `McpLifecycle` class with `spawn`/`teardown`/`teardownAll`/`getActive`/`listActive`. Internal `Map<sporeName, { client; childPid; spawnedAt }>`. Spawn flow per spec §5.5 7-step list. Teardown SIGTERM-then-SIGKILL with grace window from `MCP_TEARDOWN_GRACE_MS` (default 2000).

- [ ] **Step 4: Run tests → expect green**

- [ ] **Step 5: Run full suite + biome + typecheck → expect clean**

- [ ] **Step 6: Commit**

```bash
GIT_AUTHOR_NAME="Patrick" GIT_AUTHOR_EMAIL="amey-jonesp@cardiff.ac.uk" \
GIT_COMMITTER_NAME="Patrick" GIT_COMMITTER_EMAIL="amey-jonesp@cardiff.ac.uk" \
git commit -m "feat(mcp): McpLifecycle multi-active server process owner with crash detection"
```

---

## Task 25 — `HitlGate` discriminated union + UI components (TDD)

**Files:**

- Modify: `src/security/hitlGate.ts`
- Modify: `src/ui/ApprovalPrompt.tsx`
- Modify: `src/ui/App.tsx` (type narrowing only — no behavior change)
- Modify: `tests/unit/security/hitlGate.test.ts`
- Create or modify: `tests/unit/ui/ApprovalPrompt.test.ts`

Refactors per spec §5.8 — `ApprovalRequest` becomes a discriminated union (`bash|write|read|mcp`); `checkBash`/`checkWrite`/`checkRead` construct kind-tagged requests; new `checkMcp` method; `ApprovalPrompt.tsx` rendering branches on `kind` instead of hardcoding `command`/`cwd`.

- [ ] **Step 1: Add failing tests to `tests/unit/security/hitlGate.test.ts` + ApprovalPrompt.test.ts**

`hitlGate.test.ts` cases per spec §5.9 hitlGate.test.ts bullet:

- `checkMcp` with mock requester that approves → `{ allowed: true, requiredApproval: true }`.
- `checkMcp` with mock requester that rejects → `{ allowed: false, requiredApproval: true, feedback }`.
- The `ApprovalRequest.kind` discriminant flows through to the requester for all four kinds (bash, write, read, mcp).
- Existing `checkBash`/`checkWrite`/`checkRead` tests pass with the new kind tag (regression coverage — adapt existing assertions).

`ApprovalPrompt.test.ts` cases (using `ink-testing-library`):

- Renders bash request (Command + Cwd fields).
- Renders write request (Write to + Cwd).
- Renders read request (Read).
- Renders mcp request (Server + Tool + Args + reason).

- [ ] **Step 2: Run test → expect failures (type errors + missing checkMcp)**

- [ ] **Step 3: Implement per spec §5.8**

`hitlGate.ts`: change `ApprovalRequest` to discriminated union; update `checkBash`/`checkWrite`/`checkRead` to add `kind` tag; add `checkMcp` method (signature per spec §5.8 wire-touch #1).

`ApprovalPrompt.tsx`: replace hardcoded `request.command` (line 31) and `request.cwd` (line 33) with the `switch (request.kind)` rendering branch from spec §5.8 wire-touch #2.

`App.tsx`: holds `approvalRequests: ApprovalRequest[]` (line 41) — type re-validates after the refactor; no runtime change but compile must succeed (per spec §5.8 wire-touch #3).

- [ ] **Step 4: Run tests → expect green**

Existing approval-related tests + integration tests must all pass.

- [ ] **Step 5: Run full suite + biome + typecheck → expect clean**

- [ ] **Step 6: Commit**

```bash
GIT_AUTHOR_NAME="Patrick" GIT_AUTHOR_EMAIL="amey-jonesp@cardiff.ac.uk" \
GIT_COMMITTER_NAME="Patrick" GIT_COMMITTER_EMAIL="amey-jonesp@cardiff.ac.uk" \
git commit -m "refactor(security): HitlGate ApprovalRequest discriminated union + checkMcp + UI branches"
```

---

## Task 26 — `parseSubcommand` + `mcpInstall` CLI (TDD)

**Files:**

- Create: `src/cli/parseSubcommand.ts` (subsumes existing `parseResumeFlag`/`parseNoSporeFlag` — delete those exports from `src/runtime/resume.ts`).
- Create: `src/cli/mcpInstall.ts`
- Modify: `src/runtime/resume.ts` — remove `parseResumeFlag`/`parseNoSporeFlag` exports (now internal to `parseSubcommand`'s `interactive` branch). The `isSafeToResume`/`buildTurnsFromHistory` exports stay.
- Modify: `src/index.ts:54-56` — replace the two `parseFooFlag` calls with single `parseSubcommand(...)` + discriminated dispatch.
- Create: `tests/unit/cli/parseSubcommand.test.ts`
- Create: `tests/unit/cli/mcpInstall.test.ts`

Phase 3 introduces the first subcommand. Per spec §5.6: `parseSubcommand` returns a discriminated union AND **subsumes** the existing flag parsers — don't run them in sequence (they conflict on `myceliate mcp install foo --resume bar`). `mcpInstall` runs the atomic install flow with staging-dir + POSIX `rename(2)` per Gemini's atomic-install fix.

- [ ] **Step 1: Add failing tests to `tests/unit/cli/parseSubcommand.test.ts`**

- `parseSubcommand([])` → `{ kind: 'interactive', noSpore: false }`.
- `parseSubcommand(['--resume', 'abc'])` → `{ kind: 'interactive', resumeId: 'abc', noSpore: false }`.
- `parseSubcommand(['--no-spore'])` → `{ kind: 'interactive', noSpore: true }`.
- `parseSubcommand(['mcp', 'install', 'playwright', '--command', 'npx', '--arg', '@playwright/mcp@latest'])` → `{ kind: 'mcp-install', name: 'playwright', command: 'npx', args: ['@playwright/mcp@latest'], env: {}, regenerate: false }`.
- `parseSubcommand(['mcp', 'install', 'foo', '--command', 'bar', '--env', 'KEY=VAL'])` → `env: { KEY: 'VAL' }`.
- `parseSubcommand(['mcp', 'install', 'foo'])` (no `--command`) → throws or returns parse error.
- Regression: existing `parseResumeFlag` test cases move from `src/runtime/resume.test.ts` to here.

- [ ] **Step 2: Add failing tests to `tests/unit/cli/mcpInstall.test.ts`**

Use the fake-server fixture from T22:

- Successful install writes `manifest.yaml`, `SKILL.md`, `commands/*.md` to target dir under tmp `HOME` prefix.
- Atomic regression: simulate failure mid-flow (kill the fake before `listTools`, or supply a nonexistent command) → no partial dir at target, staging dir cleaned up.
- `--regenerate` against existing dir preserves below-marker SKILL.md content.
- Manifest's `allowed_tools` field carries the **namespaced** form (`<spore>_<rawtool>`) per spec §5.6 step 7.

- [ ] **Step 3: Run tests → expect failures**

- [ ] **Step 4: Implement per spec §5.6**

`parseSubcommand.ts` — discriminated union parser per spec §5.6 TypeScript surface. Subsume the existing `parseResumeFlag`/`parseNoSporeFlag` logic into the `interactive` branch, then delete those exports from `src/runtime/resume.ts`.

`mcpInstall.ts` — `runMcpInstall(opts)` exported for testing + use from `index.ts`. Atomic flow per spec §5.6 step list: staging-dir write → POSIX `rename(2)` move → cleanup-on-failure via try/catch.

`src/index.ts:54-56` — replace the two `parseFooFlag` calls with `const sub = parseSubcommand(process.argv.slice(2))` + `if (sub.kind === 'mcp-install') { await runMcpInstall(sub); process.exit(0); }`.

- [ ] **Step 5: Run tests → expect green**

- [ ] **Step 6: Run full suite + biome + typecheck → expect clean**

- [ ] **Step 7: Commit**

```bash
GIT_AUTHOR_NAME="Patrick" GIT_AUTHOR_EMAIL="amey-jonesp@cardiff.ac.uk" \
GIT_COMMITTER_NAME="Patrick" GIT_COMMITTER_EMAIL="amey-jonesp@cardiff.ac.uk" \
git commit -m "feat(cli): parseSubcommand subsumes flag parsers; add atomic mcp install"
```

---

## Task 27 — `bootTools.ts` threading + `teardownMcpSpore` closure (TDD)

**Files:**

- Modify: `src/runtime/bootTools.ts`
- Modify: `tests/unit/runtime/bootTools.test.ts` (extension; if absent, create).

Per spec §5.7.3: `BootToolsOpts` gains `mcpLifecycle: McpLifecycle`. `BootToolsResult` gains `teardownMcpSpore: (sporeName: string) => Promise<void>` — the closure that bridges `tools.deregisterByPrefix` + `mcpLifecycle.teardown` + system-message `StreamEvent` emit. The same closure is wired internally as `mcpLifecycle.opts.onUnexpectedExit` AND returned in the result for callers (T29's `index.ts` + `replSession.ts`) to invoke on explicit teardown.

- [ ] **Step 1: Add failing tests to bootTools.test.ts**

- `bootTools(...)` returns `teardownMcpSpore` closure in result.
- Calling `teardownMcpSpore('playwright')` invokes `tools.deregisterByPrefix('playwright_')` AND `mcpLifecycle.teardown('playwright')` AND emits a `system_message` `StreamEvent` containing the count of removed wrappers.
- The same closure is set as `mcpLifecycle.opts.onUnexpectedExit` during `bootTools()` execution (assert by triggering a fake `onUnexpectedExit` and observing the side effects).
- `createGerminateSporeTool` receives `mcpLifecycle`/`toolRegistry`/`hitlGate` deps (use a recording stub for `createGerminateSporeTool` to verify the deps were threaded).
- Existing bootTools tests pass with the new `mcpLifecycle` opt populated.

- [ ] **Step 2: Run test → expect failures**

- [ ] **Step 3: Implement per spec §5.7.3**

Add `mcpLifecycle: McpLifecycle` to `BootToolsOpts`. Construct the `teardownMcpSpore` closure inside `bootTools()`; wire it as `mcpLifecycle.opts.onUnexpectedExit` (resolution per spec §5.7.3: either `McpLifecycle` exposes a setter for `onUnexpectedExit` post-construction, OR `bootTools` constructs `McpLifecycle` itself given the construction args; implementer picks — document inline). Add `teardownMcpSpore` to `BootToolsResult`. Update the `createGerminateSporeTool` call site at `bootTools.ts:81-100` to pass the new deps (`mcpLifecycle: opts.mcpLifecycle`, `toolRegistry: tools`, `hitlGate: opts.hitl`).

- [ ] **Step 4: Run tests → expect green**

- [ ] **Step 5: Run full suite + biome + typecheck → expect clean**

- [ ] **Step 6: Commit**

```bash
GIT_AUTHOR_NAME="Patrick" GIT_AUTHOR_EMAIL="amey-jonesp@cardiff.ac.uk" \
GIT_COMMITTER_NAME="Patrick" GIT_COMMITTER_EMAIL="amey-jonesp@cardiff.ac.uk" \
git commit -m "feat(runtime): bootTools threads mcpLifecycle deps + builds teardownMcpSpore closure"
```

---

## Task 28 — `germinate_spore.ts` MCP wrapper registration (TDD)

**Files:**

- Modify: `src/tools/germinate_spore.ts`
- Modify: `tests/unit/tools/germinate_spore.test.ts`

Per spec §5.7.2: extend `GerminateSporeDeps` with `mcpLifecycle`/`toolRegistry`/`hitlGate`; modify handler to dispatch on `spore.manifest.mcp_server` presence. When MCP-backed: spawn (idempotent), register namespaced wrappers (skipping pre-existing per §5.1.6 multi-active), then inject body. **No teardown of prior MCP-spores** (multi-active).

- [ ] **Step 1: Add failing tests to germinate_spore.test.ts**

Cases per spec §5.9 germinate_spore.test.ts bullet list:

- Germinating an MCP-spore calls `lifecycle.spawn`, registers namespaced tool wrappers in registry, injects body. (Use a stub `McpLifecycle` with a recording `spawn`.)
- Germinating a non-MCP-spore does NOT call lifecycle (regression).
- **Multi-active**: germinate playwright then postgres → BOTH spores' wrappers in registry; both clients alive; only postgres' SKILL.md body in system prompt (single-active body).
- **Idempotent re-germination**: re-germinating an already-germinated MCP-spore is a no-op for lifecycle AND skips re-registration of tool wrappers (the pre-existence check in spec §5.7.2 step 2(b) skips them).
- Failed spawn surfaces a stream-event error and does NOT corrupt registry state.
- **Crash recovery**: simulating `mcpLifecycle.onUnexpectedExit` firing → bootTools deregistration closure runs → registry no longer lists `<spore>_*` tools → system-message StreamEvent emitted with count.
- **Sensitive tool routing**: registering an MCP wrapper whose raw name is in `manifest.mcp_server.sensitive_tools` results in `hitlGate.checkMcp` being called when the wrapper runs (use a stub `HitlGate` to verify; assert the `(server, tool)` payload).
- Tool wrapper handles `McpServerCrashedError` and `McpToolTimeoutError` from `client.callTool` — returns JSON `{ ok: false, error }` to the model rather than throwing.

- [ ] **Step 2: Run test → expect failures**

- [ ] **Step 3: Implement per spec §5.7.2**

Extend `GerminateSporeDeps` interface. Modify handler per spec §5.7.2 step list:

1. Resolve target spore (existing).
2. If `spore.manifest.mcp_server`: `await mcpLifecycle.spawn(spore)`; for each MCP tool in `await client.listTools()`, register a wrapper with namespacing (`<spore>_<raw>`) + skip-if-pre-existing + sensitivity check + crash/timeout error handling. Per spec §5.7.2 step 2(b), the wrapper's `inputSchema` is `{ kind: 'json-schema', jsonSchema: tool.inputSchema }`.
3. Read SKILL.md body, inject via `appendSystemPrompt` (existing).
4. Write pin (existing).
5. Emit germination event (existing).

**Order matters**: register tools BEFORE injecting body (per spec §5.7.2 last paragraph) so the model can immediately call the named tools the body references.

**Allowlist × dynamic registration ordering** per spec §5.7.2: ensure `setActiveSporeFromGerminate` callback fires AFTER step 2 (so MCP wrappers exist before allowlist validation runs in `bootTools.setActiveSpore`).

- [ ] **Step 4: Run tests → expect green**

- [ ] **Step 5: Run full suite + biome + typecheck → expect clean**

- [ ] **Step 6: Commit**

```bash
GIT_AUTHOR_NAME="Patrick" GIT_AUTHOR_EMAIL="amey-jonesp@cardiff.ac.uk" \
GIT_COMMITTER_NAME="Patrick" GIT_COMMITTER_EMAIL="amey-jonesp@cardiff.ac.uk" \
git commit -m "feat(tools): germinate_spore registers MCP wrappers; multi-active per spec §5.1.6"
```

---

## Task 29 — `src/index.ts` + `replSession.ts` integration

**Files:**

- Modify: `src/index.ts` (construct `McpLifecycle`, pass to `bootTools`, wire `onActiveSporeChange` to the `teardownMcpSpore` closure, add `mcpLifecycle.teardownAll()` to `finally` block).
- Modify: `src/runtime/replSession.ts` (the `/spore unpin` handler at lines 151-155 invokes `teardownMcpSpore` via a callback threaded into `ReplSessionOptions`).
- Modify: `tests/unit/runtime/replSession.test.ts` (extension if exists).

Final integration task. Per spec §5.5 wired-into list. The `McpLifecycle` instance lives in `main()`; `bootTools` receives it and returns `teardownMcpSpore`; `replSession.ts` and `index.ts` consume the closure for explicit teardowns.

- [ ] **Step 1: Add failing tests to replSession.test.ts**

- `/spore unpin` invokes the configured `teardownMcpSpore` callback with the previously-pinned spore name (use a recording stub).
- Existing `/spore unpin` behavior (clearing the pin file) unchanged.
- `/spore pin <other>` does NOT invoke teardown for the previously-active spore (multi-active per §5.1.6).

- [ ] **Step 2: Run test → expect failures**

- [ ] **Step 3: Implement per spec §5.5 wired-into bullet list**

`src/index.ts`:

- Near the `bootTools(...)` call: `const mcpLifecycle = new McpLifecycle({ logger, callTimeoutMs: ... })`.
- Pass `mcpLifecycle` into `bootTools` opts.
- Receive `teardownMcpSpore` from result.
- Modify `onActiveSporeChange` callback (line 290): when new active spore is `null` (unpin), invoke `await teardownMcpSpore(prevSporeName)` if the previously-active spore had `mcp_server`. When new active spore is non-null (pin to a different spore), do NOT teardown (multi-active).
- `finally` block (lines 439-455): insert `await mcpLifecycle.teardownAll()` BEFORE `worker.shutdown()`.

`src/runtime/replSession.ts`:

- Add `teardownMcpSpore?: (sporeName: string) => Promise<void>` to `ReplSessionOptions`.
- In the `/spore unpin` handler (line 151-155): before calling `opts.onActiveSporeChange?.(null)`, call `await opts.teardownMcpSpore?.(activeSporeName)` for the previously-pinned spore.

- [ ] **Step 4: Run tests → expect green**

Run targeted tests: `pnpm test tests/unit/runtime/replSession.test.ts tests/unit/runtime/bootTools.test.ts`.

- [ ] **Step 5: Run full suite + biome + typecheck → expect clean**

- [ ] **Step 6: Commit**

```bash
GIT_AUTHOR_NAME="Patrick" GIT_AUTHOR_EMAIL="amey-jonesp@cardiff.ac.uk" \
GIT_COMMITTER_NAME="Patrick" GIT_COMMITTER_EMAIL="amey-jonesp@cardiff.ac.uk" \
git commit -m "feat(runtime): wire MCP lifecycle into index.ts boot path + replSession unpin"
```

---

## Task 30 — Integration tests (recording-adapter pattern) (TDD)

**Files:**

- Create: `tests/integration/RecordingMcpClient.ts` — drop-in `McpClient` impl that captures every call.
- Create: `tests/integration/mcpEndToEnd.test.ts`
- Create: `tests/integration/mcpAllowlist.test.ts`

Phase 2's recording-adapter pattern (Task 17) transferred to MCP. The tests assert MCP translation reaches the wire: from a model tool_call to a Recording MCP client `callTool` invocation, with the orchestrator history reflecting the result.

- [ ] **Step 1: Implement `tests/integration/RecordingMcpClient.ts`**

A class implementing the `McpClient` interface that:

- `initialize`: no-op resolves.
- `listTools`: returns a configurable `predeclaredTools` array.
- `callTool(name, args)`: pushes `{ name, args, ts: Date.now() }` onto an internal `calls` array; returns a configurable `cannedResult` (defaults to `{ content: [{ type: 'text', text: 'ok' }], isError: false }`).
- `close`: no-op resolves.
- `isFaulted`: returns `false` (no crash modeling in the recording variant).
- `onUnexpectedExit`: stores the handler but never invokes.
- Exposes `getCalls(): Array<{name, args, ts}>` for assertion.

- [ ] **Step 2: Implement `tests/integration/mcpEndToEnd.test.ts`**

End-to-end "translation reaches the wire" assertion per spec §5.9 last new bullet:

- Use `tests/fixtures/mcp/fake-server.mjs` as the actual spawned server (we want the real wire path exercised once, not all-mock).
- Programmatically run install: `await runMcpInstall({ name: 'fake-spore', command: 'node', args: ['tests/fixtures/mcp/fake-server.mjs'], ... })` against a tmp `HOME` override.
- Verify `~/.myceliate/skills/fake-spore/{manifest.yaml, SKILL.md, commands/*.md}` exist.
- Mount `runReactLoop` against a recording DeepSeek adapter that emits a `tool_call` against `fake-spore_echo` on iteration 0.
- Assert: (a) recording DeepSeek adapter saw `fake-spore_echo` in tool definitions; (b) the tool_call landed on the real MCP server; (c) the orchestrator history contains a `tool_result` message with the canned response content.

- [ ] **Step 3: Implement `tests/integration/mcpAllowlist.test.ts`**

The `allowed_tools` × dynamic registration ordering case (subagent M4 per spec §5.7.2):

- Install MCP-spore with `allowed_tools: ['<spore>_navigate']` (namespaced form per spec §5.6 step 7).
- Germinate the spore.
- Verify wrapper is registered in the registry AND visible through the allowlist filter (no `allowlist_unknown_tool` warning fires; capture logger calls to assert).
- Regression: hand-authored spore with `allowed_tools: ['bash']` still works (existing single-active allowlist semantics unchanged).

- [ ] **Step 4: Run tests → expect green**

Run: `pnpm test:integration`
Expected: both new integration tests pass; existing Phase 2 integration tests still pass.

- [ ] **Step 5: Run full suite + biome + typecheck → expect clean**

- [ ] **Step 6: Commit**

```bash
GIT_AUTHOR_NAME="Patrick" GIT_AUTHOR_EMAIL="amey-jonesp@cardiff.ac.uk" \
GIT_COMMITTER_NAME="Patrick" GIT_COMMITTER_EMAIL="amey-jonesp@cardiff.ac.uk" \
git commit -m "test(integration): MCP end-to-end + allowlist x dynamic registration"
```

---

## Task 31 — Doc and config updates

**Files:**

- Modify: `CLAUDE.md` (R5 amendment + line 188 lift, per spec §5.11).
- Modify: `README.md` (architecture paragraph per spec §5.11).
- Modify: `.env.example` (3 new MCP env vars per spec §5.11).
- Modify: `package.json` (verify `@modelcontextprotocol/sdk` pinned per T22).
- Modify: `docs/MANUAL_SMOKE.md` (walk-point 10 per spec §5.10, full text).

No code changes; documentation alignment with what landed across T20–T30.

- [ ] **Step 1: Apply `CLAUDE.md` changes**

R5 amendment: replace the existing "MCP is reserved for v2 external integrations..." text per spec §5.11 first bullet (full new text in spec).
Line 188: replace "MCP server integration" with the lifted-but-constrained note per spec §5.11 second bullet.

- [ ] **Step 2: Apply `README.md` change**

Insert the new architecture paragraph per spec §5.11 third bullet.

- [ ] **Step 3: Apply `.env.example` change**

Append the three MCP env-var lines per spec §5.11 (`MCP_INITIALIZE_TIMEOUT_MS`, `MCP_TEARDOWN_GRACE_MS`, `MCP_CALL_TIMEOUT_MS`).

- [ ] **Step 4: Apply `MANUAL_SMOKE.md` walk-point 10**

Append walk-point 10 from spec §5.10 verbatim (steps 1–9 covering install, atomic regression, germination, multi-active, cross-domain, explicit teardown, crash recovery, force-quit, postgres setup, hang protection).

- [ ] **Step 5: Verify `@modelcontextprotocol/sdk` pinned correctly in `package.json`**

Confirm version is pinned to a specific minor (not open-ended `^x.y.z`). Note any concerning transitive deps.

- [ ] **Step 6: Commit**

```bash
GIT_AUTHOR_NAME="Patrick" GIT_AUTHOR_EMAIL="amey-jonesp@cardiff.ac.uk" \
GIT_COMMITTER_NAME="Patrick" GIT_COMMITTER_EMAIL="amey-jonesp@cardiff.ac.uk" \
git commit -m "docs(exoenzyme): CLAUDE.md R5 + README + .env.example + MANUAL_SMOKE walk-point 10"
```

---

## Task 32 — Manual smoke regression (verification only, no commit)

**Files:** none modified — Patrick's verification before two-stage parallel review.

End-to-end smoke per spec §5.10 walk-point 10 + v1.4/v1.5 prior walk-points.

- [ ] **Step 1: Walk-point 10 step 1 — Playwright install + atomic regression**

`myceliate mcp install playwright --command npx --arg '@playwright/mcp@latest'`. Inspect `~/.myceliate/skills/playwright/{manifest.yaml, SKILL.md, commands/*.md}`. Re-run with bad `--command` → confirm no half-written dir at target, staging dir cleaned up.

- [ ] **Step 2: Walk-point 10 step 2 — Playwright germination + use**

`myceliate` → "open https://example.com and read me the page title." Confirm: germination card, MCP server spawn within 5s, `mcp-playwright.log` created, `playwright_navigate` + `playwright_snapshot` calls land, model returns title.

- [ ] **Step 3: Walk-point 10 step 3 — Multi-active behavior on `/spore pin`**

`/spore pin <other-spore>`. Confirm playwright wrappers STILL in `/spore tools` output, MCP server still alive (`ps aux | grep playwright`), no body for playwright in system prompt.

- [ ] **Step 4: Walk-point 10 step 4 — Cross-domain workflow**

(After installing postgres per step 8 below.) Pin postgres, run multi-step task using both → both servers stay alive, tool calls reach both.

- [ ] **Step 5: Walk-point 10 step 5 — Explicit teardown via `/spore unpin`**

`/spore unpin` → postgres server terminates within 2s, `postgres_*` wrappers gone, system-message visible in chat. Playwright wrappers stay (it's not active).

- [ ] **Step 6: Walk-point 10 step 6 — Crash recovery**

`kill -9` the playwright child manually. Confirm: orchestrator detects within ms, deregisters wrappers, system-message visible.

- [ ] **Step 7: Walk-point 10 step 7 — Force-quit cleanup**

Ctrl+C twice. Confirm `ps aux | grep -E 'playwright|server-postgres'` shows no surviving children.

- [ ] **Step 8: Walk-point 10 step 8 — Postgres setup + sensitive tool gating**

`docker run -d -p 5432:5432 -e POSTGRES_PASSWORD=test postgres:16` + `myceliate mcp install postgres --command npx --arg '@modelcontextprotocol/server-postgres' --arg postgresql://postgres:test@localhost:5432/postgres`. Manifest hand-edit: `mcp_server.sensitive_tools: ['execute_query']`. Confirm reads (`list_tables`/`describe_table`) don't prompt; writes (`execute_query`) DO prompt with kind:'mcp' approval card.

- [ ] **Step 9: Walk-point 10 step 9 — Hang protection**

Trigger `SELECT pg_sleep(60)` with `MCP_CALL_TIMEOUT_MS=5000`. Confirm tool_result with `isError: true` carrying `McpToolTimeoutError` message after ~5s. Postgres server stays alive (timeout doesn't trigger teardown — only crashes do).

- [ ] **Step 10: v1.4 + v1.5 walk-points 2-9 regression**

Quickly re-run prior walk-points (file edits, bash dispatch, HITL bash approval, Cortina write/read approvals, sector spore germination, anamorph routing patterns in `session.log`). Confirm Phase 3 is strictly additive for non-MCP code paths — no behavioural regression expected.

If everything passes: Phase 3 implementation is complete and ready for two-stage parallel review (general-purpose × 2, sonnet — spec compliance + code quality, per `feedback_myceliate_workflow.md`).

---

## Phase 4 — Anastomosis (stub)

Agent-controlled `compact_state` tool. The Spore writes a structured summary to `.myceliate/memory/anchors.md` at logical task boundaries; the platform safely clears the context window and re-injects the summary as a fresh anchor.

Estimated tasks: 5-7. Estimated LOC: ~300-500 (compact_state tool + anchor file management + context clear/re-inject in QueryEngine). Full task breakdown when Phase 4 design amendment lands.
